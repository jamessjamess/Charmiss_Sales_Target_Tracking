# Change Request: อนุมัติในแต่ละขั้น + ออกแบบหน้า Top-down ใหม่ + รองรับ Channel ใหม่ + TT แบ่งตาม Sales Person (สำหรับ Claude Code)

> สั่ง Claude Code: "อ่าน PROMPT_change_approval-topdown-v3.md และ CLAUDE.md แล้วสรุปไฟล์ที่จะแก้และลำดับงานก่อนเริ่ม"
> ทำหลังจาก Change Request ก่อนหน้า (top-down-v2, phasing-v2, sku-v2) เสร็จแล้ว

ยังคงยึดหลักการเดิมใน `CLAUDE.md` ทั้งหมด (Script ธรรมดา, `window.SP`, Module ไม่เรียกกันเอง, สูตรอยู่ที่ `calc.js`, ข้อความอยู่ที่ `content.js`, ดับเบิลคลิกเปิดได้, ไม่ต้องเลื่อนที่ 1920×1080, ห้ามใช้ Library กราฟ)

ลำดับงานที่แนะนำ: ข้อ 1 (Channel แบบ Config) → ข้อ 2 (TT ตาม Sales Person) → ข้อ 3 (Workflow + ปุ่มแก้ไข/บันทึก/ยืนยัน) → ข้อ 4 (หน้า Top-down ใหม่) → ข้อ 5 (ติดตั้ง Workflow ในหน้าอื่น) → ข้อ 6 (หน้าสรุป)

---

## 1. Channel แบบ Config (รองรับ Channel ใหม่ เช่น Export)

- ย้ายนิยาม Channel ทั้งหมดไปที่ `data/channels.js` (Channel Master) **ห้าม Hardcode `MT` / `TT` / `ECOM` ในโค้ดของ Module หรือ core** ทุกที่ที่เคยเขียนชื่อ Channel ตรงๆ ต้องอ่านจาก Master
- โครงของแต่ละ Channel:
  ```js
  { id, name, fullName, colorToken,        // colorToken เช่น "--ch-1"
    allocationUnit: "ACCOUNT" | "SALES_PERSON",
    sellOutMethod: "ACTUAL" | "SELL_IN_MINUS_CN",
    hasGP: true | false,
    active: true | false, order }
  ```
- ข้อมูลตั้งต้น: MT (ACCOUNT), TT (SALES_PERSON, SELL_IN_MINUS_CN, hasGP false), ECOM (ACCOUNT), **Export (ACCOUNT, active: false)** เพื่อใช้เดโมการเพิ่ม Channel
- สี Channel กำหนดเป็น `--ch-1` ถึง `--ch-8` ใน `tokens.css` Channel ใหม่ได้สีถัดไปตาม `order`
- Channel ในแผนของปีเก็บที่ `plan.<year>.topDown.channels` (รายการ id ที่เปิดใช้ในปีนั้น)
- ทุก Module ที่มี Filter Channel (`subChannelPicker`, Phasing, SKU, Product Master) ต้องแสดง Channel ตามแผนของปีแบบ Dynamic และรองรับ 5 Channel ขึ้นไปโดย Layout ไม่พัง
- คำว่า "Sub-channel" ใน UI เปลี่ยนเป็น **"หน่วยแบ่งเป้า"** ในที่ที่ครอบคลุมทั้ง Account และ Sales Person (ในโค้ดใช้ชื่อกลาง `unit`, `unitId`, `unitType`) ป้ายหัวคอลัมน์ให้แสดงตาม `allocationUnit` ของ Channel นั้น: `Account` หรือ `Sales Person`

## 2. TT แบ่งเป้าตาม Sales Person

- Channel ที่ `allocationUnit = "SALES_PERSON"` ใช้รายชื่อจาก `data/salespeople.js` (เฉพาะคนที่ `channel` ตรงกับ Channel นั้นและ `active`) แทน Account
- ปุ่มเพิ่มในชั้นนี้เป็น `+ เพิ่ม Sales Person` เลือกจาก Master เท่านั้น
- เป้าของ Sales Person คือ **Quota** ของคนนั้น การแบ่งลงสาขาเป็นงานของ Sales Person เอง ระบบไม่ต้องลงถึงสาขา
- Channel แบบ SALES_PERSON **ไม่มีช่องเลือก Sales Person** ในหน้า Phasing / SKU (เพราะหน่วยคือคนอยู่แล้ว) Channel แบบ ACCOUNT ยังมีช่องเลือก Sales Person ต่อ Account เหมือนเดิม
- เพิ่ม History ปีก่อนต่อ Sales Person ของ TT ใน `data/history.js` (สมมติ 4–5 คน)
- หน้า Phasing และหน้าวางแผน SKU ของ TT ใช้ Sales Person เป็นหน่วย (โครงข้อมูลเดิมใช้ได้ เพียงเปลี่ยนจาก Account เป็น Sales Person)

## 3. Workflow ต่อขั้น + ปุ่มแก้ไข / บันทึก / ยืนยัน

### 3.1 ยุบหน้าอนุมัติ

- ตั้งหน้า `approval` เป็น `visible: false` **ห้ามลบไฟล์** ย้ายคำอธิบาย Baseline / Forecast ไปไว้ใน Decision log ของหน้าสรุป
- การอนุมัติไปอยู่ในแต่ละขั้นแทน

### 3.2 หน่วยอนุมัติและผู้อนุมัติ

| ขั้น | หน่วยที่อนุมัติ | ผู้ส่ง | ผู้อนุมัติ |
|---|---|---|---|
| Top-down | ทั้งหน้า (Total + Channel + หน่วยแบ่งเป้า) | Sales Director | Management |
| Phasing | ต่อหน่วยแบ่งเป้า | Sales Person / KAM ของหน่วยนั้น | Sales Director |
| วางแผน SKU | ต่อหน่วยแบ่งเป้า | Sales Person / KAM ของหน่วยนั้น | Sales Director |
| Baseline ทั้งปี | ทั้งปี (ทำที่หน้าสรุป) | – | Sales Director กดล็อกได้เมื่อ SKU ทุกหน่วยอนุมัติแล้ว |

### 3.3 สถานะ

`ร่าง` → `รออนุมัติ` → `อนุมัติแล้ว` และ `ส่งกลับแก้` (ผู้อนุมัติต้องใส่เหตุผล) และ `ต้องตรวจใหม่` (ดูข้อ 3.5)

- แก้ไขได้เฉพาะสถานะ `ร่าง` และ `ส่งกลับแก้`
- `อนุมัติแล้ว` ถ้าต้องแก้ ผู้อนุมัติกด `เปิดให้แก้` กลับเป็น `ร่าง`

### 3.4 ปุ่มและโหมดแก้ไข (กันมือไปโดน)

- ทุกหน้าที่มีตัวเลขเปิดมาเป็น **โหมดดู** ตัวเลขเป็นข้อความธรรมดา ไม่มีช่องกรอก
- ปุ่มตามสถานะ (สร้างเป็น Component กลาง `SP.core.components.workflowBar` วางมุมขวาของหัวหน้า):

| สถานะ / โหมด | ปุ่มที่แสดง |
|---|---|
| ร่าง (โหมดดู) | `แก้ไข` · `ส่งอนุมัติ` |
| โหมดแก้ไข | `บันทึก` · `ยกเลิก` (ยกเลิกคืนค่าก่อนเข้าโหมดแก้ไข) |
| รออนุมัติ | ผู้ส่ง: `ดึงกลับ` · ผู้อนุมัติ: `อนุมัติ` · `ส่งกลับแก้` |
| อนุมัติแล้ว | ผู้อนุมัติ: `เปิดให้แก้` |

- `ส่งอนุมัติ` และ `อนุมัติ` ต้องมีกล่องยืนยันที่สรุปตัวเลขหลัก และ **ห้ามส่งอนุมัติถ้ายังมี Remaining ไม่ครบ** (ปุ่มกดไม่ได้พร้อมข้อความบอกเหตุผล)
- ออกจากหน้าหรือเปลี่ยนหน่วยแบ่งเป้าขณะอยู่ในโหมดแก้ไขและยังไม่บันทึก ให้ถามยืนยันก่อน
- แสดงป้ายสถานะ + ผู้ส่ง/ผู้อนุมัติ + เวลา ข้างชื่อหน้า และประวัติการเปลี่ยนสถานะแบบพับได้

### 3.5 ความสัมพันธ์ระหว่างขั้น

- ขั้นล่าง **ทำร่างได้เลย** ไม่ต้องรอขั้นบน (ให้ทีมทำงานพร้อมกันได้) แต่ **ส่งอนุมัติได้เมื่อขั้นบนอนุมัติแล้ว** (Phasing ของหน่วยต้องรอ Top-down, SKU ของหน่วยต้องรอ Phasing ของหน่วยนั้น)
- ถ้าขั้นบนถูก `เปิดให้แก้` แล้วอนุมัติใหม่โดยเป้าของหน่วยนั้นเปลี่ยน ขั้นล่างที่อนุมัติไปแล้วของหน่วยนั้นเปลี่ยนเป็น `ต้องตรวจใหม่` (ต้องส่งอนุมัติอีกครั้ง)

### 3.6 Role จำลอง

- เพิ่ม Dropdown `ดูในบทบาท` ใน Header: Management / Sales Director / Sales Person (เลือกชื่อ) เก็บที่ `ui.role` ปุ่มใน `workflowBar` แสดงตามบทบาท
- ข้อความเล็กข้าง Dropdown: `จำลองบทบาทสำหรับเดโม`

### 3.7 โค้ดและข้อมูล

- Logic ของ Workflow อยู่ใน `core/workflow.js` เป็น pure functions (ไม่แตะ DOM): `allowedActions(state, role, context)`, `transition(state, action, payload)`, `canSubmit(step, unitId, year)`, `invalidateDownstream(...)`
- เก็บที่ `plan.<year>.workflow.<step>.<unitId|"all">` = `{ status, history: [{ action, by, at, note }] }`
- Test ใน `tests/calc.test.html` (หรือแยก `tests/workflow.test.html`):
  1. ร่าง + Sales Director (Top-down) → แก้ไข, ส่งอนุมัติ
  2. รออนุมัติ + Management → อนุมัติ, ส่งกลับแก้
  3. ส่งกลับแก้โดยไม่มีเหตุผล → ไม่ยอมรับ
  4. Phasing ของหน่วยส่งอนุมัติขณะ Top-down ยังไม่อนุมัติ → ไม่ยอมรับ
  5. Remaining ไม่ครบ → ส่งอนุมัติไม่ได้
  6. Top-down อนุมัติใหม่และเป้าของหน่วย A เปลี่ยน → Phasing และ SKU ของหน่วย A ที่อนุมัติแล้วกลายเป็น ต้องตรวจใหม่ หน่วยอื่นไม่เปลี่ยน

## 4. ออกแบบหน้า Top-down ใหม่ (ลดความรก)

### 4.1 ปัญหาที่ต้องแก้จากเวอร์ชันปัจจุบัน

การ์ด 3 ใบมีคอลัมน์มากเกินพื้นที่ จนเกิด Scrollbar แนวนอนและแนวตั้งในการ์ด ชื่อหน่วยตัดบรรทัด ชื่อ Sales Person แทรกทุกแถว ปุ่ม "ใช้สัดส่วนปีก่อน" ซ้ำหลายจุด และช่องกรอกแสดงตลอดเวลา

### 4.2 Layout ใหม่

```
┌ หัวหน้า: ขั้นที่ 1/n  แบ่งเป้า Top-down  [ป้ายสถานะ]            [workflowBar] ┐
├ แถบ Total (บรรทัดเดียว): Total Target · ปีก่อน · Growth · Badge Remaining          ┤
├───────────────────────────────────────────────┬──────────────────────────┤
│ ตาราง Tree (กว้าง ~60%)                        │ กราฟ (กว้าง ~40%)          │
│ Channel ▸ หน่วยแบ่งเป้า                         │  • Donut สัดส่วน Channel   │
│                                               │  • Bar ยอดต่อหน่วยแบ่งเป้า  │
└───────────────────────────────────────────────┴──────────────────────────┘
```

**ตาราง Tree แทนการ์ด 3 ใบ** (รองรับ Channel จำนวนเท่าใดก็ได้):

| คอลัมน์ | โหมดดู | โหมดแก้ไข |
|---|---|---|
| ชื่อ (Channel หรือหน่วย ย่อหน้าเข้า) | ข้อความ | ข้อความ + ปุ่มลบ (×) ที่หน่วย |
| ปีก่อน (ล้าน) | ตัวเลข | ตัวเลข |
| % ของชั้นบน | ตัวเลข | ช่องกรอก (`percentAmountInput`) |
| เป้า (บาท) | ตัวเลข | ช่องกรอก (`percentAmountInput`) |
| Growth | ตัวเลขสีเขียว/แดง | ตัวเลขสีเขียว/แดง |
| Remaining | แสดงที่แถว Channel เท่านั้น (Badge) | แสดงที่แถว Channel เท่านั้น (Badge) |

- แถว Channel มีแถบสีซ้ายตาม `colorToken` กดลูกศรเพื่อพับ/กางหน่วยข้างใน ค่าเริ่มต้นกางทั้งหมด
- ตัดชื่อ Sales Person ใต้ชื่อ Account ออก (ย้ายเป็น tooltip)
- ใช้ชื่อเต็มของ Channel เป็น tooltip แสดงแค่ชื่อย่อ (MT, TT, ECOM, Export)
- โหมดแก้ไขเท่านั้น: ท้ายแต่ละ Channel มีแถว `+ เพิ่ม Account` หรือ `+ เพิ่ม Sales Person` ตาม `allocationUnit` และท้ายตารางมี `+ เพิ่ม Channel` (เลือกจาก Channel Master ที่ยังไม่อยู่ในแผน)
- ปุ่ม `ใช้สัดส่วนปีก่อน` เหลือจุดเดียว: เมนู `⋯` ที่แถว Total (แบ่ง Channel) และที่แถว Channel (แบ่งหน่วย) แสดงเฉพาะโหมดแก้ไข
- ตารางยาวเกินจอ ให้เลื่อนภายในพื้นที่ตาราง หัวตารางติดบน **ห้ามมี Scrollbar แนวนอน**
- ความสูงแถวประมาณ 34px ตัวเลขชิดขวา ใช้ `font-variant-numeric: tabular-nums`

### 4.3 กราฟ (SVG ที่เขียนเอง ห้ามใช้ Library)

สร้างเป็น Component กลางใน `core/charts.js` (`SP.core.charts.donut`, `SP.core.charts.barList`) เพื่อใช้ซ้ำในหน้าอื่น

1. **Donut สัดส่วน Channel**
   - แต่ละชิ้น = เป้าของ Channel สีตาม `colorToken` กลางวงแสดง Total (ล้าน)
   - Legend ด้านข้าง: ชื่อ · % · ล้าน · เทียบสัดส่วนปีก่อน (เช่น `45% (ปีก่อน 45.5%)`)
   - Hover ชิ้นแล้วไฮไลต์แถว Channel ในตาราง และกลับกัน
2. **Bar แนวนอน ยอดต่อหน่วยแบ่งเป้า**
   - 1 แท่งต่อหน่วย จัดกลุ่มตาม Channel (หัวกลุ่มเล็ก) เรียงจากมากไปน้อยในกลุ่ม สีตาม Channel
   - เส้นขีดบนแท่ง = ยอดปีก่อน ตัวเลขท้ายแท่ง = เป้า (ล้าน) · Growth
   - มี Filter เล็ก `ทั้งหมด | MT | TT | …` เหนือกราฟ (สร้างจาก Channel ในแผน)
   - หน่วยมากจนล้น ให้เลื่อนภายในกล่องกราฟ
- กราฟอัปเดตทันทีเมื่อตัวเลขในโหมดแก้ไขเปลี่ยน (หลัง Enter / blur)

### 4.4 ข้อมูลตั้งต้น

คงตัวเลขเดิม (Total 120 ล้าน, MT 45%, TT 20%, ECOM 35%) โดย TT เปลี่ยนเป็น Sales Person 3–4 คน และยังมี Alert ครบ 3 แบบ

## 5. ติดตั้ง Workflow ในหน้า Phasing และวางแผน SKU

- ใส่ `workflowBar` ที่หัวหน้า ทำงานต่อหน่วยแบ่งเป้าที่เลือกอยู่
- ใน `subChannelPicker` แต่ละตัวเลือกแสดงสถานะ Workflow (ไอคอนเล็ก) เพิ่มจากจุดสี Remaining
- หน้า SKU: โหมด `สร้างแผนครั้งแรก | ปรับแผน` ยังคงอยู่ การปรับแผน (Re-forecast) มี Workflow แยกของตัวเอง และใช้ได้หลังล็อก Baseline แล้วเท่านั้น (ก่อนล็อกให้ปุ่มโหมดปรับแผนกดไม่ได้พร้อมข้อความบอก)
- ตัดกล่อง "สิ่งที่ต้องการให้อนุมัติ" ของทุกหน้าที่ยังเหลือ (ย้ายข้อความไปหน้าสรุป)

## 6. หน้าสรุป

- เพิ่มส่วนแรก **สถานะการอนุมัติ**: ตาราง แถว = หน่วยแบ่งเป้า (จัดกลุ่มตาม Channel) · คอลัมน์ = Top-down / Phasing / SKU · ช่อง = ป้ายสถานะ กดแล้วไปหน้านั้นของหน่วยนั้น
- ปุ่ม `ล็อก Baseline ปี 2027` สำหรับ Sales Director กดได้เมื่อ SKU ทุกหน่วยอนุมัติแล้ว (มีกล่องยืนยัน) หลังล็อกแสดงวันเวลาที่ล็อก
- ตาราง "เป้าและแผนต่อ Sub-channel" ให้ใช้หัวคอลัมน์ `หน่วยแบ่งเป้า` และรองรับ Channel แบบ Dynamic
- อัปเดต Decision log: การอนุมัติต่อขั้น, ปุ่มแก้ไข/บันทึก/ยืนยัน, Channel แบบ Config, TT ตาม Sales Person (Quota) และลบข้อที่ขัดกับของใหม่
- เพิ่มในคำถามที่ค้าง: `แผน SKU ของ TT ทำที่ระดับ Sales Person ใช่หรือไม่` และ `CN% ของ TT กำหนดต่อ Sales Person หรือทั้ง Channel`
- กล่อง "สิ่งที่ต้องการให้อนุมัติ" ท้ายหน้าสรุป (การอนุมัติ Concept ของ Prototype) คงไว้

## 7. สิ่งที่ห้ามทำ

- ห้าม Hardcode ชื่อหรือจำนวน Channel
- ห้ามแสดงช่องกรอกในโหมดดู
- ห้ามส่งอนุมัติเมื่อ Remaining ไม่ครบ
- ห้ามใช้ Library กราฟ
- ห้ามลบไฟล์ของหน้า `approval`
- ห้ามเขียน Logic Workflow ใน Module ให้เรียก `core/workflow.js`

## 8. ตรวจก่อนส่งงาน

1. Test ทั้งหมดผ่าน (รวม Workflow 6 ข้อ)
2. หน้า Top-down ที่ 1920×1080 ไม่ต้องเลื่อน ไม่มี Scrollbar แนวนอน และอ่านง่ายกว่าเดิมอย่างชัดเจน
3. กด `+ เพิ่ม Channel` เลือก Export แล้ว Export ปรากฏในตาราง, Donut, Bar, Filter ของหน้า Phasing / SKU / Product Master
4. TT แสดงเป็น Sales Person ทุกหน้า และไม่มีช่องเลือก Sales Person ซ้ำ
5. เปิดหน้ามาเป็นโหมดดู กด แก้ไข → เปลี่ยนตัวเลข → ยกเลิก แล้วค่ากลับเป็นค่าเดิม
6. เล่น Workflow ครบวงด้วยการสลับบทบาท: Sales Director ส่ง Top-down → Management อนุมัติ → Sales Person ส่ง Phasing → Sales Director อนุมัติ → … → ล็อก Baseline ที่หน้าสรุป
7. เปิด Top-down ให้แก้แล้วเปลี่ยนเป้าหน่วยหนึ่ง อนุมัติใหม่ หน่วยนั้นในหน้าสรุปเป็น `ต้องตรวจใหม่`
8. อัปเดต `CLAUDE.md` (Channel Master, `unit`, Workflow, `workflowBar`, `core/charts.js`, Role จำลอง)
