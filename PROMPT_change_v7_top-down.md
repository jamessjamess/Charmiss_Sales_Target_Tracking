# Change Request v7: ปรับหน้า Top-down (สัดส่วน, การเติบโต, กราฟ, Export, Platform) (สำหรับ Claude Code)

> สั่ง Claude Code: "อ่าน PROMPT_change_v7_top-down.md และ CLAUDE.md แล้วสรุปไฟล์ที่จะแก้ก่อนเริ่ม"
> ทำหลังจาก v6 เสร็จแล้ว

ยังคงยึดหลักการเดิมใน `CLAUDE.md` ทั้งหมด และมาตรฐานกลางของ v5 (ภาษาทางการ, ตารางคำศัพท์, สีสถานะ, รูปแบบตัวเลข, โครงหัวหน้า, `workflowBar`, แถวคงเหลือ)

ลำดับงาน: ข้อ 1 (ชื่อหน่วยตาม Channel) → ข้อ 2 (สัดส่วน 2 คอลัมน์) → ข้อ 3 (การเติบโต) → ข้อ 4 (กราฟ) → ข้อ 5 (Export) → ข้อ 6 (ข้อมูลตั้งต้น)

---

## 1. ชื่อหน่วยตาม Channel (ECOM = Platform)

- เพิ่มฟิลด์ใน Channel Master (`data/channels.js`):
  - `unitLabel` — ชื่อเรียกหน่วยใน Channel: MT = `Account`, TT = `เขตการขาย`, ECOM = `Platform`, Export = `Account`
  - `gpLabel` — ชื่อเรียก GP: MT = `GP`, ECOM = `ค่าธรรมเนียม Platform`, Export = `GP` (TT ไม่มี เพราะ `hasGP = false`)
- ทุกที่ที่แสดงชื่อหน่วยหรือ GP ให้อ่านจาก 2 ฟิลด์นี้ ห้าม Hardcode เช่น
  - ปุ่มในแถว Channel: `+ Account` / `+ เขต` / `+ Platform`
  - Dropdown ในแถบบริบทหน้า Phasing และวางแผน SKU: ป้าย `Account` / `เขตการขาย` / `Platform`
  - แถบบริบท: `GP 40%` / `ค่าธรรมเนียม Platform 20%`
  - แถวว่าง: `ยังไม่มี Platform`
- ข้อความรวมที่ต้องครอบคลุมทุกแบบ ใช้ `หน่วยขาย` แทน `Account / เขตการขาย` (อัปเดตตารางคำศัพท์ใน `content.js` และคำอธิบายหน้า Top-down เป็น `กำหนดเป้าหมาย Net Sales ประจำปี และจัดสรรให้แต่ละ Channel และหน่วยขาย`)
- Account Master เพิ่ม Platform ใหม่ใน ECOM แบบ `active: false`: `Website (Own)` และ `LINE Shop` เพื่อเดโมการเพิ่มในอนาคต
  - `Website (Own)` ใช้ค่าธรรมเนียม Payment เป็นค่า GP (เช่น 3%) แสดงหมายเหตุในหน้า Account ว่า `ช่องทางของบริษัท: ค่าธรรมเนียมคือค่าบริการชำระเงิน`
- ปุ่ม `+ Platform` ที่กดไม่ได้เพราะเพิ่มครบแล้ว ต้องมี Tooltip `เพิ่ม Platform ที่เปิดใช้งานครบแล้ว · เปิดใช้งาน Platform ใหม่ได้ที่ Account Master`

## 2. สัดส่วน 2 คอลัมน์ (แก้ความสับสนระหว่างระดับ Channel และหน่วยขาย)

### 2.1 ปัญหา

คอลัมน์ `สัดส่วน (%)` เดิมใช้ฐานต่างกันในแต่ละแถว (Channel เทียบ Total, หน่วยขายเทียบ Channel) ทำให้ `TT เขต 1 = 100%` ดูเหมือนได้เป้าทั้งบริษัท

### 2.2 แก้เป็น 2 คอลัมน์ที่ฐานชัดเจน

| คอลัมน์ | แถว Channel | แถวหน่วยขาย | แถวคงเหลือของ Channel | แถวคงเหลือระดับ Total |
|---|---|---|---|---|
| **% ของ Total** | **ช่องกรอก** | ตัวเลขอ่านอย่างเดียว (สีจาง) | ว่าง | % ที่ยังไม่จัดสรร |
| **% ใน Channel** | `100%` อ่านอย่างเดียว (สีจาง) | **ช่องกรอก** | % ที่ยังไม่จัดสรรใน Channel | ว่าง |

- หัวคอลัมน์ `% ใน Channel` มี Tooltip `สัดส่วนของหน่วยขายเทียบกับเป้าหมายของ Channel นั้น`
- ในโหมดแก้ไข ช่องกรอกแสดงเฉพาะตำแหน่งตามตาราง ช่องอื่นเป็นข้อความอ่านอย่างเดียว เพื่อให้เห็นชัดว่าแถวไหนแก้ที่คอลัมน์ไหน
- `% ของ Total` ของหน่วยขาย = % ใน Channel × % ของ Total ของ Channel (คำนวณใน `calc.js`)
- แถวคงเหลือของ Channel เปลี่ยนป้ายเป็น `คงเหลือใน {ชื่อ Channel}` เช่น `คงเหลือใน TT · จัดสรรครบ`
- แถวคงเหลือระดับ Total ป้าย `คงเหลือระดับ Total`
- คอลัมน์ `เป้าหมาย (บาท)` ยังแก้ได้ทั้งแถว Channel และหน่วยขายเหมือนเดิม (แปลงกลับเป็น % ในคอลัมน์ที่แก้ได้ของแถวนั้น)
- ลำดับคอลัมน์ใหม่: `ชื่อ · ยอดขายปีก่อน (ล้านบาท) · % ของ Total · % ใน Channel · เป้าหมาย (บาท) · การเติบโต · เทียบปีก่อน · จัดการ`

## 3. การเติบโตในแถบ Total

- ตัดป้าย `2026 (Actual 8M + Est.)` ออกจากแถบ
- แสดงเป็น: `Total Target [ช่องกรอก] บาท · ยอดขายปี 2026 113,200,000 บาท ⓘ · การเติบโต +6.0% (+6,800,000 บาท)`
  - `ⓘ` Tooltip: `ยอดขายปี 2026 = ยอดขายจริง ม.ค.–ส.ค. + ประมาณการ ก.ย.–ธ.ค.` (เดือนตัดยอดคำนวณจากข้อมูล ไม่ Hardcode)
  - ส่วนต่างเป็นบาท แสดงเครื่องหมาย + / − และสีเดียวกับ % การเติบโต
- หัวคอลัมน์ในตาราง `ยอดขายปีก่อน (ล้านบาท)` เปลี่ยนเป็น `ยอดขายปี 2026 (ล้านบาท)` (ปีคำนวณจากปีแผน − 1) และมี `ⓘ` Tooltip เดียวกัน
- ใช้รูปแบบเดียวกันในหน้า Phasing (แถบบริบทและแถวยอดขายปีก่อน) และหน้ารายงานสรุปแผน

## 4. แทน Donut ด้วยกราฟที่ช่วยตัดสินใจ

### 4.1 เหตุผล

Donut วงซ้อนอ่านยาก และตัวเลขซ้ำกับตาราง สิ่งที่ผู้ใช้ต้องการรู้ในหน้านี้คือ **การเติบโตมาจาก Channel ไหน** และ **สัดส่วน Channel เปลี่ยนไปอย่างไร**

### 4.2 แผงขวาใหม่ (2 กราฟ ซ้อนแนวตั้ง)

1. **ที่มาของการเติบโต** (Waterfall แนวนอน)
   - แท่งแรก `ยอดขายปี 2026` → แท่งของแต่ละ Channel = เป้าหมาย − ยอดปีก่อนของ Channel (เพิ่ม = สีเขียว ลด = สีแดง) → แท่งสุดท้าย `Total Target 2027`
   - ตัวเลขท้ายแท่งเป็นล้านบาท มีเครื่องหมาย + / −
   - Channel ใหม่ที่ไม่มียอดปีก่อน แสดงเป็นแท่งเพิ่มทั้งหมด พร้อมป้าย `ใหม่`
   - ตัวอย่างจากข้อมูลปัจจุบัน: 113.20 → MT +8.30 → TT +7.10 → ECOM −8.60 → 120.00
2. **สัดส่วน Channel ปีก่อนเทียบปีนี้** (แท่ง 100% แนวนอน 2 แท่ง)
   - แท่งบน `2026` แท่งล่าง `2027` แบ่งสีตาม Channel ป้าย % ในแต่ละช่วง (ซ่อนป้ายถ้าช่วงแคบกว่า 6%)
   - Legend บรรทัดเดียวใต้กราฟ
- Hover แถบใดแล้วไฮไลต์แถว Channel ในตาราง และกลับกัน
- สร้างใน `core/charts.js`: `SP.core.charts.waterfall` และ `SP.core.charts.stackedShare` (ใช้ซ้ำในหน้ารายงานสรุปแผน) ลบ `donut` ออกจากหน้านี้ (คงฟังก์ชันไว้ถ้าหน้าอื่นใช้)
- กราฟอัปเดตทันทีเมื่อตัวเลขเปลี่ยนในโหมดแก้ไข (หลัง Enter / blur)

## 5. Export ตาราง (Excel / CSV)

### 5.1 ปุ่ม

- ปุ่ม `ส่งออก ▾` ในแถวหัวหน้า ข้าง `workflowBar` ใช้ได้ทั้งโหมดดูและโหมดแก้ไข (โหมดแก้ไขที่ยังไม่บันทึก ให้ถามว่า `ส่งออกค่าที่ยังไม่บันทึก` หรือ `ส่งออกค่าที่บันทึกล่าสุด`)
- ตัวเลือก: `Excel (.xlsx)` · `CSV`

### 5.2 วิธีทำ

- **CSV**: Blob + `<a download>` ใส่ BOM (ทำงานได้เมื่อเปิดจากไฟล์ในเครื่อง)
- **Excel**: โหลด SheetJS จาก `https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.18.5/xlsx.full.min.js` **เมื่อกดปุ่มครั้งแรกเท่านั้น** (ไม่โหลดตอนเปิดหน้า) ถ้าโหลดไม่ได้ (ไม่มีอินเทอร์เน็ต) แสดงข้อความ `ส่งออก Excel ไม่ได้ในขณะนี้ · ส่งออกเป็น CSV แทนได้` พร้อมปุ่ม CSV
- อัปเดตรายการไฟล์ภายนอกที่อนุญาตใน `CLAUDE.md` (เพิ่ม SheetJS แบบโหลดเมื่อใช้งาน)
- สร้างเป็น Component กลาง `SP.core.export` (`toCsv(rows, columns)`, `toXlsx(sheets, meta)`) และใช้ในหน้า Phasing และวางแผน SKU ด้วย (ปุ่มตำแหน่งเดียวกัน)

### 5.3 เนื้อหาไฟล์ (หน้า Top-down)

- ชื่อไฟล์: `Top-down_{ปี}_{สถานะ}_{YYYYMMDD}.xlsx` (สถานะเป็นภาษาอังกฤษสั้น เช่น `Draft`, `Approved`)
- ส่วนหัว (Excel: 4 แถวแรก / CSV: ไม่มี): ปีแผน · สถานะ · วันเวลาที่ส่งออก · มุมมองผู้ใช้
- คอลัมน์ (1 แถวต่อ Channel และ 1 แถวต่อหน่วยขาย แบบแบน ไม่ใช้ Merge):

| Channel | ระดับ | ประเภทหน่วย | หน่วยขาย | ผู้รับผิดชอบปัจจุบัน | ยอดขายปี 2026 (บาท) | % ของ Total | % ใน Channel | เป้าหมาย (บาท) | การเติบโต (%) | การเติบโต (บาท) |
|---|---|---|---|---|---|---|---|---|---|---|

  - `ระดับ` = `Channel` หรือ `หน่วยขาย` เพื่อให้กรองใน Excel ได้
  - ท้ายไฟล์: แถว `Total` และแถวคงเหลือ
- ตัวเลขเป็นชนิดตัวเลขจริง (ไม่ใช่ข้อความ) % เก็บเป็นทศนิยม (0.25) และจัดรูปแบบ `0.00%` ใน Excel เงินจัดรูปแบบ `#,##0`
- Excel: ตั้งความกว้างคอลัมน์ และตรึงแถวหัวตาราง

## 6. ข้อมูลตั้งต้น

ข้อมูลปัจจุบันของ TT เป็น `เขต 1 = 100%`, `เขต 2 = 0%`, `เขต 3 = 0%` ทั้งที่ทุกเขตมียอดปีก่อน ทำให้การเติบโตของเขต 1 เป็น +289.6% ให้ตั้งค่าตั้งต้นของทุก Channel เป็น **สัดส่วนตามยอดปีก่อน** (ปัดเป็น % เต็ม รวม 100%) และคงให้มีตัวอย่าง `ขาด` และ `เกิน` อย่างละ 1 Channel ตาม v5 ข้อ 7

## 7. Test

1. `% ของ Total` ของหน่วยขาย: Channel 25% × หน่วย 40% → 10.00%
2. แก้เป้าหมาย (บาท) ของหน่วยขาย 12,000,000 ใน Channel เป้าหมาย 30,000,000 → % ใน Channel = 40.00%
3. Waterfall: ปีก่อน MT 51.7 / TT 22.9 / ECOM 38.6 และเป้าหมาย 60 / 30 / 30 → +8.3 / +7.1 / −8.6 รวม +6.8 และแท่งสุดท้าย = 120.0
4. การเติบโต: 120,000,000 เทียบ 113,200,000 → +6.0% (+6,800,000)
5. `unitLabel`: ECOM → `Platform` และปุ่ม `+ Platform`
6. Export CSV: มี BOM, จำนวนแถว = Channel + หน่วยขาย + Total + คงเหลือ, % เป็นทศนิยม
7. ค่าตั้งต้นตามสัดส่วนปีก่อน: ปีก่อน 7.7 / 8.7 / 6.5 → 34% / 38% / 28% (รวม 100%)

## 8. สิ่งที่ห้ามทำ

- ห้ามใช้คอลัมน์สัดส่วนคอลัมน์เดียวที่ฐานต่างกันในแต่ละแถว
- ห้าม Hardcode คำว่า Account / เขต / Platform / GP
- ห้ามโหลด SheetJS ตอนเปิดหน้า
- ห้ามใช้ Merge cell ในไฟล์ Export
- ห้ามใช้ Donut ในหน้า Top-down

## 9. ตรวจก่อนส่งงาน

1. Test ทั้งหมดผ่าน
2. ในโหมดแก้ไข แถว Channel มีช่องกรอกเฉพาะ `% ของ Total` และแถวหน่วยขายมีช่องกรอกเฉพาะ `% ใน Channel`
3. แถบ Total ไม่มีป้าย `Actual 8M + Est.` และแสดงส่วนต่างเป็นบาท Tooltip อธิบายที่มาของยอดปี 2026
4. แผงขวาแสดง Waterfall และแท่งสัดส่วน 2 ปี Hover แล้วไฮไลต์แถวในตาราง
5. ECOM แสดงคำว่า Platform ทุกหน้า และเปิดใช้งาน `LINE Shop` ใน Account Master แล้วเพิ่มในหน้า Top-down ได้
6. ส่งออก Excel และ CSV ได้จากหน้า Top-down, Phasing และวางแผน SKU เปิดใน Excel แล้วภาษาไทยไม่เพี้ยน ตัวเลขคำนวณต่อได้
7. หน้า Top-down ไม่ต้องเลื่อนที่ 1920×1080
8. อัปเดต `CLAUDE.md` (`unitLabel`, `gpLabel`, สัดส่วน 2 คอลัมน์, `waterfall`, `stackedShare`, `SP.core.export`, SheetJS แบบโหลดเมื่อใช้งาน)
