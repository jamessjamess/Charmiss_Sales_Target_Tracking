Change Request: ปรับหน้า Top-down + เพิ่มการเลือกปี (สำหรับ Claude Code)
สั่ง Claude Code: "อ่าน PROMPT_change_top-down-v2.md และ CLAUDE.md แล้วสรุปไฟล์ที่จะแก้ก่อนเริ่ม"
ยังคงยึดหลักการเดิมใน `CLAUDE.md` ทั้งหมด (Script ธรรมดา, `window.SP`, Module ไม่เรียกกันเอง, สูตรอยู่ที่ `calc.js`, ข้อความอยู่ที่ `content.js`, ดับเบิลคลิกเปิดได้)
1. เลือกปีที่วางแผน

* เพิ่ม Dropdown เลือกปีใน Header (สร้างใน `core/layout.js`) ตัวเลือก 2026, 2027, 2028 ค่าเริ่มต้น 2027 (กำหนดใน `data/settings.js` เป็น `DEFAULT_PLAN_YEAR`)
* ปีที่เลือกเก็บใน `store` (Key `app.planYear`) และทุก Module อ่านจากที่นี่
* Key ของข้อมูลแผนต้องแยกตามปี เช่น `plan.2027.topDown`, `plan.2027.phasing.<subChannelId>` เปลี่ยนปีแล้วแสดงข้อมูลของปีนั้น ปีที่ไม่มีข้อมูลตั้งต้นให้เริ่มจาก 0
* History ที่แสดง = ปีก่อนหน้าปีที่เลือก (เช่น เลือก 2027 แสดง 2026)
* ปรับ Module อื่นที่อ่าน Top-down (เช่น `phasing`, `sku-planning`) ให้ใช้ Key ตามปีด้วย

2. ซ่อนหน้า "ภาพรวม" และ "ใครดูแลข้อมูลอะไร"

* ตั้ง `visible: false` ใน `core/registry.js` ห้ามลบไฟล์ (เผื่อเปิดกลับภายหลัง)
* Stepper และเมนูแสดงเฉพาะหน้าที่ `visible: true` ลำดับขั้นนับใหม่อัตโนมัติ (Top-down จะเป็นขั้นที่ 1)
* `index.html` ที่ root ให้พาไปหน้าแรกที่ `visible: true` อัตโนมัติ

3. ข้อความหัวหน้า (กระชับ)
แก้ใน `data/content.js`:

* ป้ายขั้น: `ขั้นที่ 1/6` (คำนวณจาก registry)
* หัวข้อ: `แบ่งเป้า Top-down`
* คำอธิบาย 1 บรรทัด: `ตั้ง Total Target (Net Sales) แล้วแบ่งลง Channel และ Sub-channel แก้ได้ทั้ง % และบาท`

4. ตัดกล่อง "สิ่งที่ต้องการให้อนุมัติ" ออกจากหน้านี้

* ไม่แสดงในหน้า Top-down
* ย้ายข้อความ 3 ข้อเดิมไปรวมใน Decision log ของหน้า `summary` (ผ่าน `content.js`) เพื่อไม่ให้ประเด็นหายไป
* ตัดแถบ Legend สี (ครบ / ยังจัดสรรไม่ครบ / เกินเป้า) ใต้การ์ด เพราะ Badge บอกความหมายในตัวอยู่แล้ว

5. แก้ได้ทั้ง % และบาท (ทุกชั้น)

* ทุกแถวของ Channel และ Sub-channel มีช่องกรอก 2 ช่อง: `%` และ `บาท`
   * แก้ % → บาท = % × เป้าชั้นบน
   * แก้บาท → % = บาท ÷ เป้าชั้นบน
* ค่าที่เก็บจริงคือ % (บาทคำนวณจาก %) เมื่อ Total หรือเป้า Channel เปลี่ยน ชั้นล่างคงสัดส่วน % เดิม และบาทปรับตาม
* เก็บ % แบบความละเอียดเต็ม แสดง % 2 ตำแหน่ง แสดงบาทเป็นจำนวนเต็มคั่นหลักพัน ขณะพิมพ์ในช่องบาทรองรับการพิมพ์ตัวเลขที่มีหรือไม่มีจุลภาค
* อัปเดตค่าเมื่อกด Enter หรือออกจากช่อง (blur) ไม่คำนวณทุกครั้งที่พิมพ์ เพื่อไม่ให้ตัวเลขกระโดดระหว่างพิมพ์
* สร้างเป็น Component กลางใน `core/components.js` (เช่น `SP.core.components.percentAmountInput`) เพราะหน้า Phasing จะใช้ซ้ำ
* สูตรแปลง % ↔ บาท อยู่ใน `calc.js` และเพิ่ม Test ใน `tests/calc.test.html`:
   * เป้าชั้นบน 54,000,000 กรอก 40% → 21,600,000
   * เป้าชั้นบน 54,000,000 กรอก 18,900,000 → 35.00%
   * เก็บ 40% แล้วเปลี่ยนเป้าชั้นบนเป็น 60,000,000 → 24,000,000

6. เพิ่ม / ลบ Sub-channel จาก Account Master

* สร้าง `data/accounts.js` เป็น Account Master: `{ id, name, channel: "MT"|"TT"|"ECOM", active }` ใส่ตัวอย่าง Account ให้มากกว่าที่แสดงในหน้าตั้งต้น (MT: 7-Eleven, Watsons, EVEANDBOY, Beautrium, CJexpress, Konvy, Lotus's, Tsuruha, MR.DIY / TT: Distributor ภาคเหนือ, ภาคอีสาน, ภาคกลาง, ภาคใต้ / ECOM: Shopee, Lazada, Tiktok)
* ท้ายรายการของแต่ละ Channel มีปุ่ม `+ เพิ่ม Account` เปิดรายการให้เลือกจาก Account Master ที่ `active` อยู่ใน Channel นั้นและยังไม่ถูกเพิ่ม ห้ามพิมพ์ชื่อเองแบบอิสระ
* Account ที่เพิ่มใหม่เริ่มที่ 0% และแสดง History เป็น `–` และ Growth เป็น `ใหม่` ถ้าไม่มีข้อมูลปีก่อน
* แต่ละแถวมีปุ่มลบ (×) ถ้าแถวนั้นมีค่ามากกว่า 0 ให้ถามยืนยันก่อนลบ
* รายการ Account ที่เลือกเก็บใน `plan.<year>.topDown` ด้วย และหน้า Phasing / SKU ต้องเห็น Account ที่เพิ่มใหม่

7. History ปีก่อน

* เพิ่มข้อมูลใน `data/history.js`: Net Sales ปีก่อนต่อ Channel และต่อ Account (ข้อมูลสมมติ) ของปี 2025 และ 2026
* ปี 2026 ยังไม่จบปี ให้เก็บเป็น Actual ม.ค.–ส.ค. + Estimate ก.ย.–ธ.ค. และแสดงป้ายเล็กว่า `2026 (Actual 8M + Est.)` ข้อความป้ายเก็บใน `content.js`
* ในหน้า Top-down ทุกแถว (Total, Channel, Sub-channel) แสดง:
   * `ปีก่อน` (บาท แสดงเป็นล้าน)
   * `Growth` = (เป้า ÷ ปีก่อน − 1) เป็น % สีเขียวถ้าบวก สีแดงถ้าติดลบ
* เพิ่มปุ่มเล็ก `ใช้สัดส่วนปีก่อน` ต่อ Channel (และที่ระดับ Total สำหรับแบ่ง Channel) เพื่อเติม % ตามสัดส่วนปีก่อนให้ในคลิกเดียว (Account ที่ไม่มีข้อมูลปีก่อนได้ 0%)
* สูตร Growth และสัดส่วนปีก่อนอยู่ใน `calc.js` พร้อม Test:
   * เป้า 54,000,000 ปีก่อน 48,000,000 → Growth 12.50%
   * ปีก่อน 3 Account = 10, 30, 60 → สัดส่วน 10%, 30%, 60%

8. Layout ให้อยู่ในหน้าเดียว
เป้าหมาย: หน้าจอ 1920×1080 ที่ Zoom 100% ไม่ต้องเลื่อนเลย และ 1366×768 เลื่อนน้อยที่สุด (ทดสอบทั้งสองขนาด)

* Header + Stepper รวมเป็นแถวเดียว: ชื่อระบบ, Stepper แบบชื่อสั้นบรรทัดเดียว, Dropdown ปี, ปุ่มรีเซ็ต, เมนู
* หัวหน้า: ป้ายขั้น + หัวข้อ + คำอธิบาย อยู่ในบรรทัดเดียวหรือสองบรรทัด ลดขนาดหัวข้อลง
* แถบ Total บรรทัดเดียว: `Total Target [ช่องกรอก] บาท | = 120.00 ล้าน | ปีก่อน xx ล้าน | Growth x% | แถบความคืบหน้า | Badge Remaining | ปุ่มใช้สัดส่วนปีก่อน`
* การ์ด Channel 3 คอลัมน์ เป็นตารางกระชับ:
   * แถวหัวการ์ด: ชื่อ Channel | ปีก่อน | % | บาท | Growth
   * แถว Account: ชื่อ | ปีก่อน | % | บาท | Growth | ×
   * ท้ายการ์ด: `+ เพิ่ม Account` | แถบความคืบหน้า (บาง) | Badge Remaining บรรทัดเดียว
   * ความสูงแถวประมาณ 36px ช่องกรอกเล็กลง ตัวเลขชิดขวา
   * ถ้า Account ใน Channel ใดมีมากกว่า 6 แถว ให้เลื่อนภายในการ์ด (ความสูงการ์ดคงที่) หน้าไม่ยืดตาม
* ปุ่มก่อนหน้า/ถัดไป ย่อเป็นแถบเล็กติดล่างจอ (sticky) ตัดข้อความ "ใช้ปุ่มลูกศร…" ออก
* ใช้ CSS Variables ที่มีอยู่ ห้าม Hardcode สี จอแคบกว่า 1024px ให้การ์ดเรียงลงเป็นคอลัมน์เดียวได้ (ยอมให้เลื่อน)

9. ข้อมูลตั้งต้นของปี 2027
ใช้ตัวเลขเดิมเป็นค่าตั้งต้น (Total 120,000,000 / MT 45% / TT 20% / ECOM 35% และ % ของ Account เดิม) เพื่อให้ยังเห็น Alert ครบ 3 แบบ และตั้ง History ปี 2026 ให้ Growth ของแต่ละแถวมีทั้งบวกและติดลบ
10. สิ่งที่ห้ามทำ

* ห้ามลบไฟล์ของหน้าที่ซ่อน
* ห้ามให้พิมพ์ชื่อ Account เองนอก Account Master
* ห้ามคำนวณ % / บาท / Growth ใน Module ให้เรียกจาก `calc.js`
* ห้ามเพิ่มเกณฑ์ % ของ Remaining ที่ Config ได้ (Alert มีแค่ ครบ / ยังจัดสรรไม่ครบ / เกินเป้า)

11. ตรวจก่อนส่งงาน

1. Test ใน `tests/calc.test.html` ผ่านทั้งหมด (รวม Test ใหม่ในข้อ 5 และ 7)
2. ดับเบิลคลิกเปิดจากเครื่องใน Chrome หน้าแรกที่เปิดคือ Top-down ปี 2027
3. หน้า Top-down ไม่ต้องเลื่อนที่ 1920×1080
4. แก้ % แล้วบาทเปลี่ยน แก้บาทแล้ว % เปลี่ยน เปลี่ยน Total แล้ว % ชั้นล่างคงเดิม
5. เพิ่ม Account แล้วไปหน้า Phasing เห็น Account นั้น
6. เปลี่ยนปีเป็น 2028 แล้วกลับมา 2027 ข้อมูลที่แก้ไว้ยังอยู่
7. อัปเดต `CLAUDE.md` (Key ตามปี, Account Master, Component % ↔ บาท)
