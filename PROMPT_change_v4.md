Change Request v4: ผู้รับผิดชอบตามช่วงเวลา + Side Menu 3 ระบบ + Filter Series, โหมดแก้ไข และคำอธิบายการคำนวณในหน้าวางแผน SKU (สำหรับ Claude Code)
สั่ง Claude Code: "อ่าน PROMPT_change_v4.md และ CLAUDE.md แล้วสรุปไฟล์ที่จะแก้และลำดับงานก่อนเริ่ม" ทำหลังจาก `PROMPT_change_approval-topdown-v3.md` เสร็จแล้ว ข้อ 1 ของไฟล์นี้แทนที่ข้อ 2 (TT แบ่งเป้าตาม Sales Person) ของ v3
ยังคงยึดหลักการเดิมใน `CLAUDE.md` ทั้งหมด (Script ธรรมดา, `window.SP`, Module ไม่เรียกกันเอง, สูตรอยู่ที่ `calc.js`, ข้อความอยู่ที่ `content.js`, ดับเบิลคลิกเปิดได้, ไม่ต้องเลื่อนที่ 1920×1080, ห้ามใช้ Library กราฟ)
ลำดับงาน: ข้อ 1 → ข้อ 2 → ข้อ 3 (รวม 3.4 คำอธิบายการคำนวณ) → ข้อ 4
1. แยก "หน่วยแบ่งเป้า" ออกจาก "คน" และเก็บผู้รับผิดชอบตามช่วงเวลา
1.1 เหตุผล
ถ้าแบ่งเป้าตรงไปที่ตัว Sales Person เมื่อคนนั้นลาออก เป้า ยอดขายย้อนหลัง และ History ปีก่อนจะผูกอยู่กับคนที่ไม่อยู่แล้ว และคนใหม่ไม่มี History ให้เทียบ จึงให้ เป้าและยอดขายผูกกับหน่วยที่ไม่เปลี่ยน (Account หรือเขตการขาย) และเก็บว่า ใครรับผิดชอบหน่วยนั้นในเดือนไหน แยกไว้ Performance รายคนคำนวณจากช่วงเดือนที่คนนั้นรับผิดชอบ
การเปลี่ยนผู้รับผิดชอบ ไม่ต้องแก้ Forecast หรือ Target เพราะตัวเลขอยู่กับหน่วยเดิม
1.2 เปลี่ยน Channel Master

* `allocationUnit` เหลือ 2 แบบ: `"ACCOUNT"` และ `"TERRITORY"` (ตัด `"SALES_PERSON"` ของ v3 ออก)
* TT ใช้ `"TERRITORY"` สร้าง `data/territories.js`: `{ id, channelId, name, active }` ตัวอย่าง 5 เขต เช่น `TT เขต 1 · ภาคเหนือ`, `TT เขต 2 · ภาคอีสาน`, `TT เขต 3 · ภาคกลาง`, `TT เขต 4 · ภาคตะวันออก`, `TT เขต 5 · ภาคใต้`
* ปุ่มเพิ่มในหน้า Top-down ของ TT เป็น `+ เพิ่มเขต` เลือกจาก Territory Master
* History ปีก่อนของ TT ผูกกับเขต (ย้ายข้อมูลใน `data/history.js`)

1.3 ผู้รับผิดชอบตามช่วงเวลา (ใช้กับทุก Channel)

* `data/assignments.js` และ Key `master.assignments`: `{ unitId, salesPersonId | null, fromMonth, toMonth | null }` (`null` ใน salesPersonId = ว่าง, `null` ใน toMonth = ยังรับผิดชอบอยู่)
* `data/salespeople.js` เพิ่ม `startMonth`, `endMonth` (เดือนลาออก ถ้ามี)
* กฎ:
   * 1 หน่วย ต่อ 1 เดือน มีผู้รับผิดชอบได้ 1 คน (หรือว่าง) ช่วงเวลาห้ามทับกัน
   * แก้ช่วงของเดือนที่ผ่านไปแล้วไม่ได้ (เป็นประวัติ)
   * เปลี่ยนผู้รับผิดชอบได้ทุกเดือนที่ยังไม่ผ่าน ไม่ติดการล็อก M+1 ถึง M+3 (คนลาออกไม่รอรอบ Forecast)
   * ถ้า Sales Person มี `endMonth` แล้วยังมีหน่วยที่ผูกหลังจากนั้น ให้แสดง Alert `มีหน่วยที่ยังผูกกับคนที่ลาออก` และถือเป็น `ว่าง` ในการคำนวณ
* ฟังก์ชันใน `calc.js`:
   * `ownerOf(unitId, month)` → salesPersonId หรือ null
   * `performanceByPerson(personId, year, source)` → รวมเป้าและยอดของทุกหน่วย เฉพาะเดือนที่คนนั้นรับผิดชอบ (`source` = Target Baseline หรือ Actual)
* เอาช่องเลือก Sales Person แบบค่าเดียวออกจาก `subChannelPicker` เปลี่ยนเป็นแสดง ผู้รับผิดชอบปัจจุบัน (อ่านอย่างเดียว) และ Tooltip แสดงประวัติ
* หน้า Phasing และหน้าวางแผน SKU เพิ่มแถวบนสุดของตาราง `ผู้รับผิดชอบ` แสดงชื่อต่อเดือนเป็นแถบสี (เดือนที่คนเดียวกันต่อเนื่องรวมเป็นแถบเดียว) ช่วงว่างเป็นลายเส้นทแยง
* Key เดิม `plan.<year>.salesPerson.<unitId>` ให้แปลงเป็น assignment ทั้งปีตอนโหลด แล้วเลิกใช้

1.4 Test

1. หน่วย A: คน X ม.ค.–มิ.ย., คน Y ก.ค.–ธ.ค. → `ownerOf(A, พ.ค.)` = X, `ownerOf(A, ส.ค.)` = Y
2. `performanceByPerson(X)` รวมเฉพาะ ม.ค.–มิ.ย. ของหน่วย A
3. ช่วงเวลาทับกัน → ไม่ยอมรับ
4. แก้ assignment ของเดือนที่ผ่านแล้ว → ไม่ยอมรับ
5. คน X `endMonth` = มิ.ย. แต่ยังผูกถึง ธ.ค. → ก.ค.–ธ.ค. นับเป็นว่าง และมี Alert
6. เปลี่ยนผู้รับผิดชอบแล้ว Target Baseline และ Forecast ของหน่วยไม่เปลี่ยน

2. Side Menu 3 ระบบ
2.1 โครงเมนู
เปลี่ยนเมนู Dropdown มุมขวาและ Stepper ใน Header เป็น Side Menu ด้านซ้าย 3 กลุ่ม:

```
Sales Planning
  1 แบ่งเป้า Top-down
  2 กระจายเป้ารายเดือน
  3 วางแผนราย SKU
  4 สรุปและสถานะอนุมัติ
Product Master
  • Listing และวันเริ่มขาย
Account Master
  • Account
  • เขตการขาย (TT)
  • Sales Person และผู้รับผิดชอบ

```

* เพิ่มฟิลด์ `group` ใน `core/registry.js` (`"sales-planning" | "product-master" | "account-master"`) เมนูสร้างจาก registry อัตโนมัติ
* ลำดับขั้น "ขั้นที่ x/n" และปุ่มก่อนหน้า/ถัดไป นับเฉพาะหน้าในกลุ่ม Sales Planning หน้าในกลุ่มอื่นไม่มีปุ่มก่อนหน้า/ถัดไป
* ย้าย Product Master · Listing ออกจาก Tour ของ Sales Planning ไปอยู่กลุ่ม Product Master
* หน้า Status SKU (`sku-status`), หน้า Approval และ หน้าจากจำนวนชิ้นถึง Net Sales (`measure-chain`) ตั้ง `visible: false` ห้ามลบไฟล์ เนื้อหาของหน้า `measure-chain` ย้ายไปอยู่ในหน้าวางแผน SKU ตามข้อ 3.4
* ข้อความกฎ Status และวิธีเติมยอด ย้ายไปเป็นแผง "กฎ Status และวิธีเติมยอด" แบบพับได้ (ค่าเริ่มต้นพับ) ในหน้า Product Master และปุ่ม `?` ข้าง Legend ในหน้าวางแผน SKU ที่เปิดแผงเดียวกัน (สร้างเป็น Component กลาง ข้อความจาก `content.js`)
* Header เหลือ: ชื่อระบบ · ปีแผน · บทบาทจำลอง · รีเซ็ตข้อมูล

2.2 พฤติกรรม Side Menu

* กว้างประมาณ 220px พับเหลือไอคอน ~56px ได้ (ปุ่มพับที่ล่างเมนู) จำสถานะไว้ที่ `ui.sidebarCollapsed`
* หน้าวางแผน SKU พับเมนูให้อัตโนมัติถ้าจอกว้างน้อยกว่า 1920px เพื่อให้ 12 เดือนพอดีจอ
* ไฮไลต์หน้าปัจจุบัน และแสดงไอคอนสถานะ Workflow เล็กๆ ข้างชื่อหน้าใน Sales Planning (ตามหน่วยที่เลือกอยู่)
* จอแคบกว่า 1024px เมนูเป็นแบบเลื่อนออกด้วยปุ่ม ☰
* `index.html` พาไปหน้าแรกของกลุ่ม Sales Planning

2.3 Module Account Master (ใหม่ 3 หน้า)
ใช้ `workflowBar` แบบง่าย (มีแค่ `แก้ไข` · `บันทึก` · `ยกเลิก` ไม่มีขั้นอนุมัติ) เปิดมาเป็นโหมดดู

1. Account — ตาราง Account ต่อ Channel: ชื่อ · Channel · GP% (มีวันที่มีผล) · Active · ผู้รับผิดชอบปัจจุบัน Filter Channel แบบ Dynamic เพิ่ม/ปิดใช้งาน Account ได้
2. เขตการขาย (TT) — ตาราง Territory: ชื่อ · Channel · Active · ผู้รับผิดชอบปัจจุบัน
3. Sales Person และผู้รับผิดชอบ
   * ซ้าย: รายชื่อ Sales Person (ชื่อ · Channel · เริ่มงาน · ลาออก · สถานะ) ปุ่ม `บันทึกการลาออก` ให้เลือกเดือนลาออก
   * ขวา: Timeline ผู้รับผิดชอบ แถว = หน่วย (Account / เขต) คอลัมน์ = 12 เดือนของปีแผน แสดงแถบสีตามคน ช่วงว่างเป็นลายเส้น ในโหมดแก้ไขเลือกช่วงเดือนแล้วกำหนดคนได้
   * ด้านบน: Alert หน่วยที่ว่างหรือผูกกับคนที่ลาออก
   * ด้านล่าง: ตาราง Performance ต่อคน (Target Baseline · Actual · % ความสำเร็จ) คำนวณจาก `performanceByPerson` รวมคนที่ลาออกแล้วด้วย (ป้าย `ลาออก ก.ค. 2027`)

* ย้ายข้อมูลและหน้าจัดการ Account ที่อยู่ในหน้า Top-down เดิม (ถ้ามี) มาใช้ Master นี้ หน้า Top-down ยังเพิ่ม/ลบหน่วยในแผนของปีได้ แต่เลือกจาก Master เท่านั้น

3. หน้าวางแผน SKU
3.1 Filter Series

* เปลี่ยนชื่อฟิลด์ `campaign` เป็น `series` ใน Product Master (แปลงข้อมูลเดิมตอนโหลด) และแสดงคำว่า `Series` ใน UI ทุกหน้า
* เพิ่ม Filter `Series` แบบเลือกหลายค่า มีช่องค้นหา ในแถวเครื่องมือของหน้าวางแผน SKU แสดงจำนวน SKU ต่อ Series ในรายการ
* เมื่อเลือก Series:
   * ตารางแสดงเฉพาะ SKU ของ Series นั้น
   * แถวสรุปท้ายตารางแสดง 2 ชุด: `รวม Series ที่เลือก` และ `รวมทั้งหน่วย` (Remaining ยังเทียบกับเป้าทั้งหน่วยเสมอ ห้ามเทียบกับยอดเฉพาะ Series)
   * ป้าย `SKU ของ Series นี้ที่ Listing แล้วแต่ยังไม่อยู่ในแผน: n รายการ` พร้อมปุ่ม `เพิ่มทั้ง Series` (เพิ่มทุก SKU ของ Series ที่ Listing ในหน่วยนี้ ถ้าเป็น NPD ให้ใช้เดือนเริ่มขายเริ่มต้น = เดือนของ Launch Date)
* Filter Series ใช้ในหน้า Product Master · Listing ด้วย (Component เดียวกัน `SP.core.components.seriesFilter`)
* จำค่าที่เลือกไว้ที่ `ui.seriesFilter`

3.2 ต้องกดแก้ไขก่อนจึงแก้ได้

* เปิดหน้ามาเป็น โหมดดู: ทุกช่องเป็นตัวเลขธรรมดา ไม่มีกรอบช่องกรอก ไม่มีปุ่มลบ (×) ไม่มี `+ เพิ่ม SKU` / `เพิ่มทั้ง Series` / Dropdown เดือนเริ่มขาย
* ใช้ `workflowBar` จาก v3: `แก้ไข` → โหมดแก้ไข (แสดงช่องกรอกตามกฎ `cellState`) → `บันทึก` หรือ `ยกเลิก`
* โหมดแก้ไขแสดงแถบสีบางด้านบนตาราง `กำลังแก้ไข · ยังไม่บันทึก n ช่อง`
* ช่องที่แก้แล้วแต่ยังไม่บันทึก มีขอบหนาขึ้นจนกว่าจะบันทึก
* เปลี่ยนหน่วย, เปลี่ยน Filter Series, เปลี่ยนโหมด (สร้างแผน / ปรับแผน) หรือออกจากหน้า ขณะมีช่องที่ยังไม่บันทึก ให้ถามยืนยันก่อน
* ใช้หลักเดียวกันกับหน้า Top-down และ Phasing (ถ้ายังไม่เป็นแบบนี้ ให้แก้ให้เหมือนกัน)

3.3 Legend
ย่อ Legend ให้เหลือบรรทัดเดียว และเพิ่มปุ่ม `?` เปิดแผงกฎ Status และวิธีเติมยอด (ข้อ 2.1)
3.4 คำอธิบายการคำนวณ (ย้ายมาจากหน้า `measure-chain`)
เป้าหมายคือลดจำนวนขั้นใน Sales Planning โดยให้คำอธิบายสูตรอยู่ตรงที่ใช้ตัวเลขจริง แทนการมีหน้าแยก
1) ไอคอน ⓘ ข้างปุ่มมุมมอง `จำนวนชิ้น | Sell-out Amount | Net Sales`
กดแล้วเปิด Popover เล็ก (ไม่ใช่ Modal เต็มจอ ปิดด้วยการคลิกข้างนอกหรือ Esc) มี 3 ส่วน:

* สูตร (ข้อความจาก `content.js`): 

```
Sell-out Amount ก่อน VAT = จำนวนชิ้น × ราคาNet Sales                = Sell-out Amount ก่อน VAT × (1 − GP)Sell-out Amount รวม VAT  = Sell-out Amount ก่อน VAT × 1.07

```

พร้อมบรรทัดเล็ก: `ราคาเดือนที่มี Promo บางวัน ใช้ราคาเฉลี่ยถ่วงตามจำนวนวัน`
* เงินที่ลูกค้าจ่ายไปอยู่ที่ใคร ของหน่วยที่เลือก (ทั้งปีตามแผน): แถบแนวนอนแบ่ง 3 ส่วน Net Sales (บริษัท) · GP (ร้านค้า) · VAT พร้อมตัวเลขและ % ใช้สีเดิมจากหน้า `measure-chain` ถ้า Channel มี `hasGP = false` ให้ไม่แสดงส่วน GP
* ค่าที่ใช้กับหน่วยนี้: GP% ที่มีผล · VAT 7% · `ราคาใน Price List: ไม่รวม VAT` (ตาม Setting `PRICE_INCLUDES_VAT`) และลิงก์เล็ก `คำถามค้าง: ราคารวม VAT หรือไม่` ที่พาไปหน้าสรุป

2) Tooltip รายช่อง (ในโหมดดูและโหมดแก้ไข)
Hover ช่องตัวเลขของ SKU (หรือโฟกัสด้วยคีย์บอร์ด) แล้วแสดงการคำนวณของช่องนั้นด้วยตัวเลขจริง:

```
SKU B · เม.ย. 2027 · Shopee
8,730 ชิ้น × 59.00 บาท = 515,070.00   (Sell-out ก่อน VAT)
× (1 − 20%)            = 412,056.00   (Net Sales)
รวม VAT                = 551,124.90
ที่มา: ระบบเติม · Promo 10/30 วัน (ราคาเฉลี่ย)

```

* บรรทัด `ที่มา` บอก Source ของช่อง (ล็อก 0 / กรอกเอง / ระบบเติม / Clearance / Override / Actual) และบอก Promo ถ้ามี
* ช่องล็อก 0 แสดงเหตุผล เช่น `ก่อนเดือนเริ่มขายใน Account นี้` หรือ `ไม่ได้ Listing`
* Tooltip แสดงเหมือนกันทุกมุมมอง (จำนวนชิ้น / Sell-out Amount / Net Sales)

3) โค้ด

* สร้าง Component กลาง `SP.core.components.calcExplainer` (Popover) และ `SP.core.components.cellBreakdown` (Tooltip) ตัวเลขทั้งหมดเรียกจาก `calc.js` ห้ามคำนวณใน Component
* ย้ายกราฟแถบ "เงินที่ลูกค้าจ่ายไปอยู่ที่ใคร" จาก Module `measure-chain` มาเป็นฟังก์ชันใน `core/charts.js` (`SP.core.charts.splitBar`) แล้วให้ `measure-chain` (ที่ซ่อนไว้) เรียกใช้ตัวเดียวกัน
* ข้อความ "สิ่งที่ต้องการให้อนุมัติ" 2 ข้อของหน้า `measure-chain` ย้ายไปรวมใน Decision log ของหน้าสรุป (ถ้ายังไม่มี) และคำถามเรื่อง VAT อยู่ในคำถามค้างของหน้าสรุปที่เดียว

4) Test เพิ่มใน `tests/calc.test.html`

1. `cellBreakdown` ของ 100 ชิ้น × 50 บาท GP 45% → 5,000.00 / 2,750.00 / 5,350.00
2. Split ของ Sell-out ก่อน VAT 5,000 GP 45% → Net Sales 2,750 (51.4%) · GP 2,250 (42.1%) · VAT 350 (6.5%) รวม 5,350
3. Channel ที่ `hasGP = false` → Split ไม่มีส่วน GP

4. หน้าสรุป

* ตารางสถานะอนุมัติ ใช้หัวคอลัมน์ `หน่วยแบ่งเป้า` และแสดงผู้รับผิดชอบปัจจุบันเป็นข้อความเล็ก
* อัปเดต Decision log: เป้าผูกกับหน่วย (Account / เขต) ไม่ผูกกับคน · ผู้รับผิดชอบเก็บตามช่วงเดือน · Performance รายคนคำนวณจากช่วงที่รับผิดชอบ · Side Menu 3 ระบบ · Series · สูตร Net Sales = Sell-out Amount ก่อน VAT × (1 − GP) แสดงผ่าน ⓘ และ Tooltip ในหน้าวางแผน SKU
* ลบคำถามค้าง `แผน SKU ของ TT ทำที่ระดับ Sales Person ใช่หรือไม่` แล้วเพิ่ม `แผน SKU ของ TT ทำที่ระดับเขตใช่หรือไม่` และ `การเปลี่ยนผู้รับผิดชอบต้องมีผู้อนุมัติหรือไม่`

5. สิ่งที่ห้ามทำ

* ห้ามผูกเป้าหรือยอดขายกับ Sales Person โดยตรง
* ห้ามแก้ assignment ของเดือนที่ผ่านไปแล้ว
* ห้ามให้การเปลี่ยนผู้รับผิดชอบไปแก้ตัวเลข Target หรือ Forecast
* ห้ามแสดงช่องกรอกหรือปุ่มเพิ่ม/ลบในโหมดดู
* ห้ามคำนวณ Remaining เทียบกับยอดเฉพาะ Series
* ห้ามลบไฟล์ของหน้าที่ซ่อน
* ห้ามทำคำอธิบายการคำนวณเป็นหน้าแยกหรือกล่องถาวรในหน้าวางแผน SKU (ใช้ ⓘ และ Tooltip เท่านั้น)

6. ตรวจก่อนส่งงาน

1. Test ทั้งหมดผ่าน (รวม Test ข้อ 1.4)
2. Side Menu แสดง 3 กลุ่ม พับได้ และหน้า Status SKU ไม่แสดงที่ไหนเลย
3. TT แสดงเป็นเขตการขายในทุกหน้า และ History ปีก่อนยังแสดงถูก
4. ที่หน้า Sales Person บันทึกให้คน X ลาออก มิ.ย. แล้วโอนเขตไปให้คน Y ตั้งแต่ ก.ค. → หน้า Phasing ของเขตนั้นแสดงแถบผู้รับผิดชอบ X ม.ค.–มิ.ย. และ Y ก.ค.–ธ.ค. ตัวเลขเป้าไม่เปลี่ยน ตาราง Performance แยกยอดของ X และ Y ถูกต้อง
5. หน้าวางแผน SKU เปิดมาไม่มีช่องกรอก กด แก้ไข แล้วจึงแก้ได้ ยกเลิกแล้วค่ากลับ
6. เลือก Series ที่มี NPD แล้วกด `เพิ่มทั้ง Series` SKU ทั้งชุดเข้าแผน และ Remaining ยังเทียบกับเป้าทั้งหน่วย
7. หน้าวางแผน SKU ที่ 1920×1080 ไม่เลื่อนแนวนอน
8. Sales Planning เหลือ 4 ขั้น (Top-down → Phasing → วางแผน SKU → สรุป) หน้าจากจำนวนชิ้นถึง Net Sales ไม่แสดงในเมนู
9. กด ⓘ ในหน้าวางแผน SKU เห็นสูตร แถบแบ่งเงินของหน่วยนั้น และค่า GP/VAT ที่ใช้ Hover ช่องตัวเลขเห็นการคำนวณของช่องนั้นตรงกับตัวเลขในตาราง
10. อัปเดต `CLAUDE.md` (Territory, assignments, `ownerOf`, `performanceByPerson`, `group` ใน registry, Side Menu, Series, `calcExplainer`, `cellBreakdown`, `splitBar`)
