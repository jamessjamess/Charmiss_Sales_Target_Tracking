# Change Request v5: UX/UI Review ทั้งระบบ (สำหรับ Claude Code)

> สั่ง Claude Code: "อ่าน PROMPT_change_v5_ux-review.md และ CLAUDE.md แล้วสรุปไฟล์ที่จะแก้และลำดับงานก่อนเริ่ม"
> ทำหลังจาก v4 เสร็จแล้ว

ยังคงยึดหลักการเดิมใน `CLAUDE.md` ทั้งหมด (Script ธรรมดา, `window.SP`, Module ไม่เรียกกันเอง, สูตรอยู่ที่ `calc.js`, ข้อความอยู่ที่ `content.js`, ดับเบิลคลิกเปิดได้, ไม่ต้องเลื่อนที่ 1920×1080, ห้ามใช้ Library กราฟ)

ลำดับงาน: ข้อ 1 (มาตรฐานกลาง) → ข้อ 2 (Top-down) → ข้อ 3 (Phasing) → ข้อ 4 (วางแผน SKU) → ข้อ 5 (รายงานสรุป) → ข้อ 6 (Master) → ข้อ 7 (ข้อมูลตั้งต้น)

---

## 1. มาตรฐานกลาง (ใช้ทุกหน้า)

### 1.1 ภาษาและคำศัพท์

- ใช้ภาษาไทยระดับทางการ ประโยคสั้น ไม่ใช้ภาษาพูด (เช่น "ลองแก้", "ได้เลย", "แก้ได้ทั้ง … และ …", "กด แก้ไข เพื่อเปลี่ยนตัวเลข")
- คำอธิบายใต้หัวข้อ **ไม่เกิน 1 บรรทัด** และบอก "หน้านี้ทำอะไร" ไม่ใช่วิธีใช้ปุ่ม (วิธีใช้ให้อยู่ใน Tooltip หรือแผง `?`)
- ศัพท์ธุรกิจคงเป็นภาษาอังกฤษตามที่ทีมใช้: Target, Net Sales, Sell-out Amount, GP, SKU, Channel, Account, Series, NPD, Forecast, Baseline
- ข้อความทั้งหมดอยู่ใน `data/content.js` ห้ามเขียนข้อความลงใน Module ตรงๆ

**ตารางคำที่ต้องเปลี่ยน** (ใช้ให้ตรงกันทุกหน้า)

| เดิม | ใหม่ |
|---|---|
| หน่วยแบ่งเป้า | Account / เขตการขาย (ในโค้ดยังใช้ `unit`) |
| % ของชั้นบน | สัดส่วน (%) — Tooltip ที่หัวคอลัมน์: `แถว Channel เทียบกับ Total Target · แถว Account/เขต เทียบกับ Channel` |
| เป้า (บาท) | เป้าหมาย (บาท) |
| ปีก่อน (ล้าน) | ยอดขายปีก่อน (ล้านบาท) |
| Growth | การเติบโต |
| % ของปี | สัดส่วนรายเดือน (%) |
| Net Sales (บาท) (แถวในหน้า Phasing) | เป้าหมาย Net Sales (บาท) |
| Growth vs ปีก่อน | การเติบโตเทียบปีก่อน |
| Remaining | คงเหลือ |
| ครบ / ยังจัดสรรไม่ครบ / เกินเป้า | จัดสรรครบ / ขาด {จำนวน} / เกิน {จำนวน} |
| ขาด 9.57 ล. / เกิน 5.09 ล. | ขาด 9,570,000 / เกิน 5,090,000 (ไม่ใช้ตัวย่อ "ล.") |
| ร่าง | ฉบับร่าง |
| ส่งกลับแก้ | ส่งกลับแก้ไข |
| ต้องตรวจใหม่ | ต้องตรวจสอบใหม่ |
| เปิดให้แก้ | เปิดให้แก้ไข |
| ดูในบทบาท · จำลองบทบาทสำหรับเดโม | มุมมองผู้ใช้ (จำลองสำหรับการนำเสนอ) |
| ผู้ส่ง: {ชื่อ} (ตอนยังเป็นฉบับร่าง) | ผู้จัดทำ: {ชื่อ} (แสดง "ผู้ส่ง" เมื่อส่งแล้วเท่านั้น) |
| กำลังแก้ไข · ยังไม่มีการเปลี่ยนแปลง | โหมดแก้ไข · ยังไม่มีรายการที่เปลี่ยนแปลง |
| กำลังแก้ไข · ยังไม่บันทึก n ช่อง | โหมดแก้ไข · มีรายการที่ยังไม่บันทึก n รายการ |
| Sales Person และผู้รับผิดชอบ (เมนู) | ผู้รับผิดชอบ |
| สรุปและสถานะอนุมัติ (เมนู) | รายงานสรุปแผน |

**หัวข้อและคำอธิบายของแต่ละหน้า**

| หน้า | หัวข้อ | คำอธิบาย |
|---|---|---|
| Top-down | แบ่งเป้า Top-down | กำหนดเป้าหมาย Net Sales ประจำปี และจัดสรรให้แต่ละ Channel และ Account / เขตการขาย |
| Phasing | กระจายเป้ารายเดือน | จัดสรรเป้าหมายรายปีเป็นรายเดือน โดยใช้สัดส่วนยอดขายปีก่อนเป็นค่าตั้งต้น |
| วางแผน SKU | วางแผนราย SKU | วางแผนจำนวนขายราย SKU รายเดือน ระบบคำนวณเป็น Net Sales และเปรียบเทียบกับเป้าหมายรายเดือน |
| รายงานสรุป | รายงานสรุปแผน {ปี} | สรุปเป้าหมาย แผนการขาย และสถานะการอนุมัติ |
| Listing | Listing และวันเริ่มขาย | กำหนดวันเริ่มขายและ Account ที่จำหน่ายสินค้าได้ |
| Account | Account | ข้อมูล Account และ GP ตามช่วงเวลาที่มีผล |
| เขตการขาย | เขตการขาย (TT) | ข้อมูลเขตการขายของ Traditional Trade |
| ผู้รับผิดชอบ | ผู้รับผิดชอบ | ผู้รับผิดชอบ Account / เขตการขายรายเดือน และผลงานรายบุคคล |

### 1.2 สีสถานะคงเหลือ (เปลี่ยนจากเดิม)

ขาดเป้ารุนแรงกว่าเกินเป้า จึงให้:

| สถานะ | เงื่อนไข | สี |
|---|---|---|
| จัดสรรครบ | คงเหลือ = 0 (±1 บาท) | เขียว |
| ขาด | คงเหลือ > 0 | **แดง** |
| เกิน | คงเหลือ < 0 | **เหลือง** |
| ยังไม่มีข้อมูล | เป้าหมายชั้นบนเป็น 0 หรือยังไม่มี Account/เขต | เทาอ่อน ข้อความ `ยังไม่กำหนด` |

- กำหนดเป็น Token `--status-complete`, `--status-short`, `--status-over`, `--status-empty` ใน `tokens.css` และใช้ทุกหน้า (ตาราง, จุดสถานะใน Dropdown, กราฟ, รายงาน)
- ทุกสถานะต้องมีข้อความกำกับ ห้ามสื่อด้วยสีอย่างเดียว

### 1.3 แถว "คงเหลือ" แทน Chip

- เลิกใช้ Chip คงเหลือในตาราง เปลี่ยนเป็น **แถวคงเหลือ** ท้ายกลุ่มที่ต้องรวมให้ครบ สร้างเป็น Component กลาง `SP.core.components.remainingRow`
- แถวคงเหลือ: ป้ายซ้าย `คงเหลือ` + สถานะเป็นข้อความ (`ขาด` / `เกิน` / `จัดสรรครบ`) · ตัวเลขอยู่ **ในคอลัมน์เดียวกับตัวเลขที่ต้องปรับ** (บาทอยู่ใต้คอลัมน์บาท % อยู่ใต้คอลัมน์ %) · พื้นแถวสีอ่อนตามสถานะ
- Chip ใช้ได้เฉพาะในที่ที่พื้นที่จำกัด เช่น รายการใน Dropdown หรือเมนูข้าง และต้องมีข้อความในตัว

### 1.4 ตัวเลข

- ในตารางที่กรอกตัวเลข แสดงเงินเป็นบาทเต็มจำนวน ไม่มีทศนิยม ในกราฟและรายงานใช้ล้านบาท 2 ตำแหน่ง และระบุหน่วยที่หัวคอลัมน์หรือแกนเสมอ
- ห้ามปนหน่วยในบรรทัดเดียวกัน (เช่น `เป้าทั้งปี 76,520,548 · ปีก่อน 20.00 ล้าน`) ให้ใช้หน่วยเดียว
- % แสดง 2 ตำแหน่งในช่องกรอก และ 1 ตำแหน่งในการเติบโต
- ห้ามตัดตัวเลขด้วย "…" คอลัมน์ต้องกว้างพอ ถ้าไม่พอให้ลดขนาดตัวอักษรของคอลัมน์นั้น
- เป้าหมายเป็น 0 ให้แสดงการเติบโตเป็น `–` (ไม่แสดง −100.0%) ไม่มียอดปีก่อนแสดง `ใหม่`
- ใช้ `font-variant-numeric: tabular-nums` และชิดขวาทุกคอลัมน์ตัวเลข

### 1.5 โครงหัวหน้า (เหมือนกันทุกหน้า)

```
[ขั้นที่ x/4]  หัวข้อ  [ป้ายสถานะ Workflow]  ประวัติ ▾                [ปุ่ม workflowBar]
คำอธิบาย 1 บรรทัด
```

- ชื่อผู้จัดทำ/ผู้ส่ง/ผู้อนุมัติ อยู่ใน `ประวัติ ▾` ไม่แสดงลอยมุมขวา
- `workflowBar` อยู่ขวาสุดของแถวหัวข้อ **ทุกหน้าที่มีตัวเลข** (Top-down, Phasing, วางแผน SKU)

### 1.6 เมื่อบทบาทแก้ไขไม่ได้

ปัจจุบันหน้า Phasing และหน้าวางแผน SKU ไม่มีปุ่มแก้ไขเมื่อดูในบทบาท Sales Director ทำให้ดูเหมือนระบบใช้งานไม่ได้ ให้แก้ดังนี้:

- ถ้าบทบาทปัจจุบันไม่มีสิทธิ์แก้ไข ให้ `workflowBar` แสดงข้อความแทนปุ่ม: `อ่านอย่างเดียว · ผู้จัดทำคือ {ชื่อผู้รับผิดชอบ}` พร้อมปุ่มรอง `สลับเป็นมุมมองผู้จัดทำ` (เปลี่ยน `ui.role` ทันที ใช้เพื่อการนำเสนอ)
- ถ้าบทบาทมีสิทธิ์อนุมัติแต่ยังไม่ถึงขั้นตอน (เช่น ยังเป็นฉบับร่าง) แสดง `รอผู้จัดทำส่งอนุมัติ`
- ใช้ Logic จาก `core/workflow.js` เท่านั้น

### 1.7 แถบบริบท (หน้า Phasing และวางแผน SKU)

ใช้ Component เดียวกัน (`subChannelPicker` ที่ปรับแล้ว) เป็น **แถวเดียว**:

```
[Channel: MT | TT | ECOM | Export]  [‹  Account/เขต ▾  ›]  ผู้รับผิดชอบ: {ชื่อ}  ·  GP {x}%  ·  เป้าหมายทั้งปี {บาท}  ·  ยอดขายปีก่อน {บาท}  ·  การเติบโต {x}%
```

- แสดงชื่อผู้รับผิดชอบ **ครั้งเดียว** ในแถวนี้ (เอาชื่อออกจากปุ่ม Dropdown ตอนปิด) แถบผู้รับผิดชอบรายเดือนในตารางแสดงเฉพาะเมื่อมีการเปลี่ยนคนระหว่างปี ถ้าคนเดียวทั้งปีให้ซ่อนแถวนั้น
- Dropdown ตอนปิดแสดงแค่ชื่อ Account/เขต ตอนเปิดแต่ละรายการแสดง: ชื่อ · ผู้รับผิดชอบ · สถานะคงเหลือ (ข้อความ + สี) · สถานะ Workflow (ข้อความ) เลิกใช้จุดและวงกลมเล็กที่ไม่มีคำอธิบาย
- GP แสดงเฉพาะ Channel ที่ `hasGP = true`

### 1.8 เมนูข้าง

- ชื่อเมนูอยู่บรรทัดเดียว (ใช้ชื่อสั้นตามตารางข้อ 1.1)
- ตอนพับเมนู แสดงไอคอนพร้อม Tooltip ชื่อหน้า และเลิกแสดงวงกลมสถานะเล็กที่มุมไอคอน ให้แสดงสถานะใน Tooltip แทน
- Header: ตัดบรรทัด `Concept Prototype · สำหรับขออนุมัติแนวคิด` ให้เหลือป้ายเล็ก `Prototype` ข้างชื่อระบบ

---

## 2. หน้า Top-down

### 2.1 ปัญหาที่พบ

1. Total Target แสดงซ้ำ 3 จุด (ช่องกรอก, `= 420.00 ล้าน`, กลาง Donut)
2. ตัวเลขเป้าหมายแสดงทั้งในตาราง และในกราฟแท่งด้านขวาซ้ำกันทุกแถว
3. Chip คงเหลือในแถว Channel อ่านยาก และไม่ได้อยู่ในคอลัมน์ที่ต้องปรับ
4. Export ไม่มี Account และเป้าหมายเป็น 0 แต่แสดง `ครบ`
5. คอลัมน์การเติบโตถูกตัด (`+259.5...`) และหัวคอลัมน์ `ปีก่อน (ล้าน)` ตัดเป็น 2 บรรทัด
6. ปุ่ม `+ เพิ่ม Account` เป็นข้อความเล็ก และการลบใช้ `×` เล็กๆ ที่ไม่ชัดเจน
7. Lazada เป้าหมาย 0 แสดงการเติบโต −100.0%

### 2.2 Layout ใหม่

```
┌ หัวหน้า ─────────────────────────────────────────────────── [workflowBar] ┐
├ แถบ Total: Total Target [ช่องกรอก/ตัวเลข] บาท · ยอดขายปีก่อน · การเติบโต        ┤
├──────────────────────────────────────────────────────┬───────────────────┤
│ ตาราง (≈75%) — รวมกราฟแท่งไว้ในตาราง                   │ Donut (≈25%)       │
└──────────────────────────────────────────────────────┴───────────────────┘
```

- **แถบ Total** แสดงตัวเลข Total ครั้งเดียว ตัด `= xxx ล้าน` ออก ตัดปุ่ม `⋯` ที่แถบนี้ (ย้าย `เติมตามสัดส่วนปีก่อน` ไปแถวคงเหลือระดับ Total ในข้อ 2.3)
- **ตัดกราฟแท่ง "เป้าต่อหน่วยแบ่งเป้า" ออกจากแผงขวา** และรวมเข้าไปในตารางเป็นคอลัมน์ `เทียบปีก่อน` (ข้อ 2.3)
- **Donut** เหลือกราฟเดียวในแผงขวา:
  - วงนอก = สัดส่วนปีนี้ วงใน (บางกว่า) = สัดส่วนปีก่อน เพื่อให้เห็นการเปลี่ยนสัดส่วน
  - กลางวงแสดง `% จัดสรรแล้ว` (ไม่แสดง Total ซ้ำ)
  - Legend แสดงแค่ชื่อ Channel และสี (ตัวเลขอยู่ในตารางแล้ว) Hover แล้วแสดงตัวเลขเป็น Tooltip
  - Hover ชิ้นแล้วไฮไลต์แถวในตาราง และกลับกัน

### 2.3 ตาราง

คอลัมน์:

| ชื่อ | ยอดขายปีก่อน (ล้านบาท) | สัดส่วน (%) | เป้าหมาย (บาท) | การเติบโต | เทียบปีก่อน | จัดการ |
|---|---|---|---|---|---|---|

- **คอลัมน์ `เทียบปีก่อน`** = แท่งเล็กแนวนอนในแถว (ความยาว = เป้าหมาย, ขีดตั้ง = ยอดปีก่อน) สเกลแยกระดับ: แถว Channel เทียบกันเอง แถว Account/เขต เทียบกันภายในทุก Channel กว้างประมาณ 140px สีตาม Channel
- **แถว Channel**: แถบสีซ้าย · ชื่อย่อ (ชื่อเต็มใน Tooltip) · ป้ายเล็ก `Account` หรือ `เขต` ย้ายไปอยู่ใน Tooltip
- **แถวคงเหลือ** (`remainingRow`) ท้ายแต่ละ Channel: `คงเหลือ · ขาด` + % ในคอลัมน์สัดส่วน + บาทในคอลัมน์เป้าหมาย และ **แถวคงเหลือระดับ Total** ท้ายตารางสำหรับการแบ่ง Channel
- **คอลัมน์ `จัดการ`** (แสดงเฉพาะโหมดแก้ไข):
  - แถว Channel: ปุ่มเล็กมีข้อความ `+ Account` (หรือ `+ เขต`) และเมนู `⋯` ที่มี `เติมตามสัดส่วนปีก่อน` และ `นำ Channel ออกจากแผน`
  - แถว Account/เขต: ปุ่มไอคอนถังขยะพร้อม Tooltip `นำออกจากแผน` และกล่องยืนยันที่บอกยอดที่จะหายไป
  - แถวคงเหลือ (เมื่อไม่ครบ): ปุ่ม `กระจายตามสัดส่วนปัจจุบัน` (ปรับ % ของทุกแถวในกลุ่มตามสัดส่วนเดิมให้รวมเป็น 100%) ต้องกดเองเท่านั้น ห้ามทำอัตโนมัติ
  - ท้ายตาราง: ปุ่ม `+ เพิ่ม Channel`
- ตัดแถว `+ เพิ่ม Account` แบบข้อความท้ายกลุ่ม (ใช้ปุ่มที่แถว Channel แทน) เพื่อลดความยาวตาราง
- โหมดดู: ไม่มีคอลัมน์จัดการ ไม่มีช่องกรอก ตัวเลขเป็นข้อความ
- Channel ที่ยังไม่มี Account/เขต แสดงแถวเดียว `ยังไม่มี Account` พร้อมปุ่ม `+ Account` (ในโหมดแก้ไข) สถานะ `ยังไม่กำหนด` (ห้ามแสดงจัดสรรครบ)

---

## 3. หน้า Phasing (กระจายเป้ารายเดือน)

### 3.1 ปัญหาที่พบ

1. ไม่มีปุ่มแก้ไข (ดูข้อ 1.6) ผู้ใช้เข้าใจว่าแก้ไม่ได้
2. ชื่อผู้รับผิดชอบแสดงซ้ำ 3 จุด (ใน Dropdown, ป้ายผู้รับผิดชอบ, แถบในตาราง)
3. แถบบริบทมี 2 แถว และปนหน่วยบาทกับล้าน
4. คงเหลือเป็น Chip แยกจากตาราง และ Chip `แก้แล้ว` ไม่บอกว่าแก้อะไร
5. จุดสีและวงกลมใน Dropdown ไม่มีคำอธิบาย
6. กราฟสูงมาก ตัวเลขในกราฟซ้ำกับแถวเป้าหมายในตาราง

### 3.2 การปรับ

- ใช้แถบบริบทแถวเดียวตามข้อ 1.7 และ `workflowBar` ตามข้อ 1.5
- **กราฟ**: ลดความสูงเหลือประมาณ 160px ไม่มีตัวเลขบนแท่ง (Tooltip เมื่อ Hover) คอลัมน์ตรงกับตาราง ป้าย `2026 (Actual 8M + Est.)` ย้ายไปเป็นคำอธิบายของแถวยอดขายปีก่อน
- **ตาราง** แถวตามลำดับ:
  1. ยอดขายปีก่อน (บาท)
  2. สัดส่วนรายเดือน (%)
  3. เป้าหมาย Net Sales (บาท)
  4. การเติบโตเทียบปีก่อน
  5. **แถวคงเหลือ** (`remainingRow`): % ใต้แถวสัดส่วน และบาทใต้แถวเป้าหมาย แสดงที่คอลัมน์รวม และแสดงรายเดือนเป็นช่องว่าง (คงเหลือคิดทั้งปี)
- ตัด Chip `แก้แล้ว` แทนด้วย: เดือนที่ต่างจากค่าตั้งต้นมีจุดเล็กมุมช่อง + Tooltip `ค่าตั้งต้น 7.91%` และปุ่ม `คืนค่าตามสัดส่วนปีก่อน` แสดงในโหมดแก้ไขเท่านั้น
- แถบผู้รับผิดชอบแสดงเฉพาะเมื่อมีหลายคนในปี (ข้อ 1.7)

---

## 4. หน้าวางแผนราย SKU

### 4.1 ปัญหาที่พบ

1. แถบเครื่องมือ 2 แถวแน่นมาก (Channel, Account, ผู้รับผิดชอบ, Series, มุมมอง, ⓘ, GP, โหมด, หมายเหตุโหมด)
2. การ์ดสรุป `Plan x / เป้า y` + แถบความคืบหน้า + Chip ซ้ำกับแถวรวมและแถวคงเหลือท้ายตาราง
3. Legend 8 รายการ รวมรายการที่ไม่เกี่ยวกับโหมดปัจจุบัน (Actual, ล็อก M+1–M+3 ในโหมดสร้างแผนครั้งแรก)
4. ไม่มีปุ่มแก้ไขในบทบาท Sales Director (ดูข้อ 1.6)
5. ป้าย `P` (Promo) อยู่คร่อมระหว่างช่อง
6. กลุ่ม `NPD · 0 รายการ` ว่างโดยไม่มีคำแนะนำ
7. มีพื้นที่ว่างใต้ตาราง ขณะที่แถวรวมไม่ติดล่าง

### 4.2 การปรับ

- **แถวที่ 1** = แถบบริบท (ข้อ 1.7)
- **แถวที่ 2** = ตัวควบคุมตาราง: `Series ▾` · `มุมมอง: จำนวนชิ้น | Sell-out Amount | Net Sales` · `ⓘ` · `โหมด: สร้างแผนครั้งแรก | ปรับแผน (Re-forecast)` · (ขวาสุด) `?` Legend
  - หมายเหตุ `ปรับแผนได้หลังล็อก Baseline ที่หน้าสรุป` ย้ายเป็น Tooltip ของปุ่มโหมดที่กดไม่ได้ ข้อความ: `ใช้งานได้หลังจากล็อก Baseline`
- **ตัดการ์ดสรุป** ด้านบน ข้อมูลอยู่ในแถวรวมและแถวคงเหลือแล้ว
- **Legend** แสดงเฉพาะรายการที่มีอยู่ในตารางปัจจุบันและเกี่ยวกับโหมดปัจจุบัน บรรทัดเดียว รายการเต็มอยู่ในแผง `?`
- **ท้ายตาราง ติดล่าง (sticky)**: `รวมแผน (ชิ้น)` · `รวมแผน Net Sales (บาท)` · `คงเหลือ` (`remainingRow` สีตามข้อ 1.2 รายเดือนและทั้งปี)
- **แถวเป้าหมาย** ติดบนใต้หัวตาราง ป้าย `เป้าหมาย Net Sales (จาก Phasing)`
- ความสูงตารางยืดตามพื้นที่ที่เหลือของจอ ไม่มีพื้นที่ว่างใต้ตาราง
- ป้าย `P` อยู่มุมบนซ้ายภายในช่องเสมอ
- กลุ่ม NPD ที่ว่าง: ข้อความ `ยังไม่มี NPD ในแผน` + (โหมดแก้ไข) ปุ่ม `+ เพิ่ม NPD` ถ้ามี NPD ที่ Listing แล้วแต่ยังไม่อยู่ในแผน ให้แสดงจำนวนในข้อความนี้แทนป้ายเตือนด้านบน
- แถวสรุปเมื่อกรอง Series (จาก v4) คงไว้ ใช้ป้าย `รวม Series ที่เลือก` และ `รวมทั้ง Account`

---

## 5. หน้ารายงานสรุปแผน (เปลี่ยนเป็นมุมมอง Report)

### 5.1 แนวคิด

หน้าเดิมผสมตัวเลขแผน, ขอบเขตโครงการ, Decision log และคำถามค้างไว้ในหน้าเดียว ทำให้อ่านยาก ให้แยก:

- หน้านี้ = **รายงานสรุปแผน** สำหรับผู้บริหารดูภาพรวมและอนุมัติ
- ขอบเขต, สิ่งที่ตัดออก, Decision log, คำถามค้าง, ขั้นต่อไป → ย้ายไปหน้าใหม่ `about-prototype` (หัวข้อ `เกี่ยวกับ Prototype`) ในเมนูกลุ่มใหม่ท้ายสุด `ข้อมูลโครงการ` และลิงก์ `เกี่ยวกับ Prototype` เล็กๆ ที่ Header

### 5.2 โครงรายงาน

แถบบนสุด: Filter `Channel` (หลายค่า) · ปุ่ม `พิมพ์ / บันทึก PDF` · (Sales Director) ปุ่ม `ล็อก Baseline {ปี}`

1. **KPI 4 ใบ** (แถวเดียว)
   - Total Target (บาท) + การเติบโตเทียบปีก่อน
   - แผน Bottom-up รวม (บาท) + % ของเป้าหมาย
   - ส่วนต่าง (บาท) สีตามข้อ 1.2
   - ความคืบหน้าการอนุมัติ `x / y Account/เขต` + แถบ
2. **เป้าหมายเทียบแผน รายเดือน**: กราฟแท่งเป้าหมาย (Top-down Phasing) + เส้นแผน Bottom-up + เส้นประยอดขายปีก่อน 12 เดือน (SVG)
3. **ตาราง Channel → Account/เขต**: ยอดขายปีก่อน · เป้าหมาย · แผน Bottom-up · ส่วนต่าง (สี + ข้อความ) · สถานะอนุมัติ 3 ขั้น (Top-down / Phasing / SKU เป็นข้อความสั้น) · ผู้รับผิดชอบ แถว Channel พับได้ คลิกชื่อแล้วไปหน้าวางแผนของ Account/เขตนั้น
4. **สัดส่วนแผนตามกลุ่มสินค้า** 2 กราฟคู่กัน: ตาม Status (Existing / NPD / Clearance) และตาม Series (Top 8 + อื่นๆ) เป็นแท่งแนวนอน
5. **เป้าหมายรายผู้รับผิดชอบ**: ตารางชื่อ · Account/เขตที่ดูแล · เป้าหมาย (คำนวณตามเดือนที่รับผิดชอบด้วย `performanceByPerson`) · แผน · ส่วนต่าง
6. **รายการที่ต้องดำเนินการ**: Account/เขตที่ยังขาด/เกิน, ยังไม่ส่งอนุมัติ, ส่งกลับแก้ไข, ต้องตรวจสอบใหม่, ยังไม่มีผู้รับผิดชอบ แต่ละรายการมีลิงก์ไปหน้าที่ต้องแก้

- ตัวเลขทั้งหมดในรายงานเป็นล้านบาท 2 ตำแหน่ง ยกเว้น KPI ที่แสดงบาทเต็ม
- Print CSS: A4 แนวนอน ซ่อนเมนูและปุ่ม ขึ้นหน้าใหม่ก่อนหัวข้อ 3 และ 5 มีหัวกระดาษ `รายงานสรุปแผน {ปี} · พิมพ์เมื่อ {วันเวลา}`
- สร้างกราฟด้วย `core/charts.js` (เพิ่ม `SP.core.charts.barLine` สำหรับข้อ 2)

---

## 6. หน้า Master (Listing, Account, เขตการขาย, ผู้รับผิดชอบ)

- ใช้โครงหัวหน้า ภาษา สี และรูปแบบตัวเลขตามข้อ 1 เหมือนหน้า Sales Planning
- ปุ่มเพิ่ม/ลบใช้รูปแบบเดียวกับหน้า Top-down (ปุ่มมีข้อความ + ไอคอนถังขยะพร้อมกล่องยืนยัน)
- หน้า Master ไม่มี `ขั้นที่ x/4`

---

## 7. ข้อมูลตั้งต้นสำหรับการนำเสนอ

ข้อมูลตั้งต้นต้องสอดคล้องกันทุกหน้า เพื่อไม่ให้ตัวเลขในการนำเสนอดูผิดปกติ (ปัจจุบันหลังแก้ Total เป็น 420 ล้าน การเติบโตเป็น +245% และแผน SKU ต่ำกว่าเป้า 74.5%)

- Total Target 2027 = 120,000,000 ยอดขายปีก่อนรวมประมาณ 110–115 ล้าน (การเติบโต 5–10%)
- แผน SKU ของแต่ละ Account/เขตต้องใกล้เป้าหมายรายเดือน (ส่วนต่างรวมไม่เกิน ±5%) โดยมีอย่างน้อย 1 Account ที่ขาด 1 Account ที่เกิน และ 1 Account ที่จัดสรรครบ
- Export อยู่ใน Channel Master แต่ยังไม่อยู่ในแผน (เพื่อเดโม `+ เพิ่ม Channel`)
- ปุ่ม `รีเซ็ตข้อมูล` ใน Header คืนค่าทุกอย่างเป็นชุดนี้

---

## 8. Test เพิ่ม/แก้

1. สถานะคงเหลือ: 500 → `ขาด` / −300 → `เกิน` / 0.5 → `จัดสรรครบ` / เป้าหมายชั้นบน 0 → `ยังไม่กำหนด`
2. การเติบโต: เป้าหมาย 0 → `–` / ไม่มียอดปีก่อน → `ใหม่`
3. `กระจายตามสัดส่วนปัจจุบัน`: 40%, 35%, 20% (รวม 95%) → 42.11%, 36.84%, 21.05% (รวม 100%)
4. `workflowBar` บทบาทไม่มีสิทธิ์ → คืนสถานะ `readOnly` พร้อมชื่อผู้จัดทำ
5. รายงาน: ผลรวมแผนตาม Status และตาม Series เท่ากับแผน Bottom-up รวม

## 9. สิ่งที่ห้ามทำ

- ห้ามแสดงตัวเลขเดียวกันซ้ำในหน้าเดียวโดยไม่มีเหตุผล (ตรวจทุกหน้า)
- ห้ามใช้ Chip คงเหลือในตาราง
- ห้ามใช้ตัวย่อ "ล." และห้ามตัดตัวเลขด้วย "…"
- ห้ามใช้สีเป็นสื่อเดียวของสถานะ
- ห้ามเขียนข้อความใน Module ตรงๆ (ใช้ `content.js`)
- ห้ามกระจายยอดคงเหลืออัตโนมัติ

## 10. ตรวจก่อนส่งงาน

1. Test ทั้งหมดผ่าน
2. อ่านข้อความทุกหน้าแล้วไม่มีภาษาพูด และคำตรงกับตารางข้อ 1.1
3. หน้า Top-down: Total แสดงครั้งเดียว ไม่มีกราฟแท่งแยก มีแถวคงเหลือทุก Channel และระดับ Total การเติบโตไม่ถูกตัด
4. คงเหลือขาดเป็นสีแดง เกินเป็นสีเหลือง ทุกหน้า
5. หน้า Phasing และวางแผน SKU ในบทบาท Sales Director เห็นข้อความอ่านอย่างเดียวและปุ่มสลับมุมมอง กดแล้วแก้ไขได้
6. ชื่อผู้รับผิดชอบแสดงครั้งเดียวต่อหน้า (ยกเว้นแถบรายเดือนเมื่อมีหลายคน)
7. หน้าวางแผน SKU แถบเครื่องมือเป็น 2 แถว ไม่มีการ์ดสรุป Legend แสดงเฉพาะรายการที่เกี่ยวข้อง แถวรวมติดล่าง
8. หน้ารายงานสรุปแผนพิมพ์เป็น PDF A4 แนวนอนได้เรียบร้อย และหน้าเกี่ยวกับ Prototype มีขอบเขต Decision log และคำถามค้างครบ
9. ทุกหน้าไม่ต้องเลื่อนที่ 1920×1080 (ยกเว้นหน้ารายงานที่เลื่อนแนวตั้งได้)
10. อัปเดต `CLAUDE.md` (ตารางคำศัพท์, Token สถานะ, `remainingRow`, กฎบทบาทอ่านอย่างเดียว, โครงรายงาน, หน้าเกี่ยวกับ Prototype)
