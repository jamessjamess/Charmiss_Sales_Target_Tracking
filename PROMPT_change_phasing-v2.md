# Change Request: ปรับหน้า Phasing (กระจายเป้ารายเดือน) (สำหรับ Claude Code)

> สั่ง Claude Code: "อ่าน PROMPT_change_phasing-v2.md และ CLAUDE.md แล้วสรุปไฟล์ที่จะแก้ก่อนเริ่ม"
> ทำหลังจาก `PROMPT_change_top-down-v2.md` เสร็จแล้ว เพราะใช้ Component และ Key ตามปีจากงานนั้น

ยังคงยึดหลักการเดิมใน `CLAUDE.md` ทั้งหมด (Script ธรรมดา, `window.SP`, Module ไม่เรียกกันเอง, สูตรอยู่ที่ `calc.js`, ข้อความอยู่ที่ `content.js`, ดับเบิลคลิกเปิดได้)

---

## 1. ข้อความหัวหน้า (กระชับ)

แก้ใน `data/content.js`:

- ป้ายขั้น: คำนวณจาก registry (เช่น `ขั้นที่ 2/6`)
- หัวข้อ: `กระจายเป้ารายเดือน`
- คำอธิบาย 1 บรรทัด: `ค่าตั้งต้นตาม Seasonality ปีก่อน แก้ได้ทั้ง % และบาท ผลรวมต้องเท่าเป้าทั้งปีของ Sub-channel`

## 2. ตัดกล่อง "สิ่งที่ต้องการให้อนุมัติ" และส่วนที่ซ้ำ

- ไม่แสดงกล่อง "สิ่งที่ต้องการให้อนุมัติ" ในหน้านี้ ย้ายข้อความ 2 ข้อเดิมไปรวมใน Decision log ของหน้า `summary` (ผ่าน `content.js`)
- ตัดส่วนพับ "ดูตัวเลขที่ใช้คำนวณ" ออก เพราะตารางในข้อ 5 แสดงตัวเลขครบแล้ว
- ตัดบรรทัดสูตร "เป้ารายเดือน = เป้าทั้งปีของ Sub-channel × สัดส่วนของเดือน" เหนือกราฟ (ใส่เป็น tooltip ของหัวตารางแทน)

## 3. Filter Channel แยกจาก Sub-channel

แถบเครื่องมือบรรทัดเดียวด้านบน เรียงตามนี้:

1. **Channel**: ปุ่มแบบ Segmented `MT | TT | ECOM`
2. **Sub-channel**: Dropdown แสดงเฉพาะ Account ของ Channel ที่เลือก และเป็น Account ที่ถูกเพิ่มในหน้า Top-down ของปีนั้นเท่านั้น
   - มีช่องค้นหาในรายการ (รองรับกรณี Account จำนวนมาก)
   - แต่ละตัวเลือกแสดง: ชื่อ Account · Sales Person · จุดสีสถานะ (ครบ / ยังจัดสรรไม่ครบ / เกินเป้า)
   - มีปุ่ม `‹` `›` ข้าง Dropdown เพื่อไป Account ก่อนหน้า/ถัดไปใน Channel เดียวกัน
3. **Sales Person** (ข้อ 4)
4. **เป้าทั้งปี** (อ่านอย่างเดียว จาก Top-down) + `ปีก่อน` + `Growth`
5. ปุ่ม `คืนค่า Seasonality ปีก่อน` + Badge `แก้แล้ว` (แสดงเมื่อต่างจากค่าตั้งต้น)
6. Badge Remaining (บาทและ %)

- Channel และ Sub-channel ที่เลือกล่าสุดเก็บใน `store` (Key `ui.phasing.selection`) กลับมาหน้านี้แล้วยังอยู่ที่เดิม
- ถ้าเปลี่ยน Channel ให้เลือก Account แรกของ Channel นั้นอัตโนมัติ ถ้า Channel นั้นยังไม่มี Account ให้แสดงข้อความพร้อมลิงก์กลับไปหน้า Top-down

## 4. Sales Person ต่อ Sub-channel

- สร้าง `data/salespeople.js`: `{ id, name, channel: "MT"|"TT"|"ECOM"|null, active }` ใส่ตัวอย่าง 6–8 คน (ชื่อสมมติ)
- เลือกจาก Dropdown เท่านั้น **ห้ามพิมพ์ชื่อเอง** แสดงเฉพาะคนที่ `active` และ `channel` ตรงกับ Channel ที่เลือก (หรือ `null` = ใช้ได้ทุก Channel)
- 1 Sub-channel มี Sales Person 1 คนต่อปี
- เก็บที่ Key `plan.<year>.salesPerson.<subChannelId>` (แยกจากข้อมูล Phasing เพราะหน้าอื่นจะใช้ด้วย)
- หน้า Top-down แสดงชื่อ Sales Person เป็นตัวอักษรเล็กใต้ชื่อ Account (อ่านอย่างเดียว) ถ้ายังไม่กำหนดแสดง `ยังไม่กำหนด`

## 5. แก้ได้ทั้ง % และ Net Sales รายเดือน

เปลี่ยนจากช่อง % ใต้กราฟ เป็น **ตาราง 12 เดือน + คอลัมน์รวม** อยู่ใต้กราฟ:

| แถว | ม.ค. … ธ.ค. | รวม |
|---|---|---|
| ปีก่อน (บาท) | อ่านอย่างเดียว | ผลรวม |
| % ของปี | ช่องกรอก | ผลรวม % (ต้อง = 100%) |
| Net Sales (บาท) | ช่องกรอก | ผลรวม (ต้อง = เป้าทั้งปี) |
| Growth vs ปีก่อน | อ่านอย่างเดียว สีเขียว/แดง | Growth ทั้งปี |

- ใช้ Component `SP.core.components.percentAmountInput` ตัวเดียวกับหน้า Top-down
  - แก้ % → บาท = % × เป้าทั้งปีของ Sub-channel
  - แก้บาท → % = บาท ÷ เป้าทั้งปีของ Sub-channel
- **ค่าที่เก็บจริงคือ % รายเดือน** เมื่อเป้าทั้งปีเปลี่ยนจากหน้า Top-down รายเดือนคงสัดส่วนเดิม บาทปรับตาม
- อัปเดตเมื่อกด Enter หรือออกจากช่อง รองรับ Tab และลูกศรซ้าย/ขวาเลื่อนระหว่างเดือน
- ไม่บังคับปรับเดือนอื่นอัตโนมัติเมื่อแก้เดือนหนึ่ง ให้ส่วนต่างไปแสดงที่ Remaining
- ค่าตั้งต้น % = สัดส่วนยอดรายเดือนของปีก่อนของ Sub-channel นั้น (ปีก่อน = ปีที่เลือก − 1 ถ้าเป็นปี 2026 ใช้ Actual 8M + Est.) ถ้า Account ไม่มีข้อมูลปีก่อน ใช้ Seasonality รวมของ Channel นั้นแทน และแสดงป้ายเล็ก `ใช้ Seasonality ของ Channel`
- เก็บที่ Key `plan.<year>.phasing.<subChannelId>` = `{ monthPct: [12 ค่า], edited: bool }`

สูตรใน `calc.js` พร้อม Test ใน `tests/calc.test.html`:
- เป้าทั้งปี 18,900,000 เดือนละ 10% → 1,890,000
- เป้าทั้งปี 18,900,000 กรอก 2,000,000 → 10.58%
- ผลรวม % รายเดือน 98% → Remaining 2% (378,000 บาท) สถานะ ยังจัดสรรไม่ครบ
- ยอดปีก่อนรายเดือน [1, 1, 2] → สัดส่วน 25%, 25%, 50%
- Account ไม่มีข้อมูลปีก่อน → ได้สัดส่วนของ Channel

## 6. กราฟ

- ลดความสูงกราฟเหลือประมาณ 200–240px
- คงแบบเดิม: แท่งทึบ = เป้าปีนี้, กรอบเส้นประ = ยอดปีก่อน (สเกลเดียวกัน)
- ตัดป้ายตัวเลขเหนือแท่งออก (ตัวเลขอยู่ในตารางแล้ว) แสดงตัวเลขเมื่อ Hover แทน
- ตัด Legend ใต้กราฟ ย้ายเป็นป้ายเล็ก 2 อันที่มุมขวาบนของกราฟ
- คอลัมน์ของกราฟต้องตรงกับคอลัมน์เดือนในตาราง (ใช้ Grid ความกว้างเดียวกัน) เพื่อให้มองแท่งกับตัวเลขในแนวเดียวกันได้

## 7. Layout ให้อยู่ในหน้าเดียว

เป้าหมายเดียวกับหน้า Top-down: **1920×1080 ที่ Zoom 100% ไม่ต้องเลื่อน** และ 1366×768 เลื่อนน้อยที่สุด

- ใช้ Header + Stepper แถวเดียว และแถบก่อนหน้า/ถัดไปแบบ sticky ติดล่างจอ ตามที่ทำในหน้า Top-down (ห้ามสร้างใหม่ ใช้จาก `layout.js`)
- ลำดับในหน้า: หัวหน้า (1–2 บรรทัด) → แถบเครื่องมือ (ข้อ 3) → การ์ดเดียวที่มีกราฟ + ตาราง
- ตัวเลขชิดขวา ช่องกรอกเล็ก ความสูงแถวตารางประมาณ 36px
- จอแคบกว่า 1024px ให้ตารางเลื่อนแนวนอนภายในการ์ดได้ (หน้าไม่เลื่อนแนวนอน)

## 8. สิ่งที่ห้ามทำ

- ห้ามให้พิมพ์ชื่อ Sales Person หรือ Account เอง
- ห้ามคำนวณ % / บาท / Growth / Seasonality ใน Module ให้เรียกจาก `calc.js`
- ห้ามปรับเดือนอื่นอัตโนมัติเพื่อให้ครบ 100%
- ห้ามสร้าง Component % ↔ บาท ตัวใหม่ ใช้ตัวเดียวกับหน้า Top-down

## 9. ตรวจก่อนส่งงาน

1. Test ใน `tests/calc.test.html` ผ่านทั้งหมด
2. หน้า Phasing ไม่ต้องเลื่อนที่ 1920×1080
3. เลือก Channel แล้ว Dropdown Sub-channel แสดงเฉพาะ Account ของ Channel นั้น ค้นหาได้ และปุ่ม ‹ › ใช้ได้
4. Account ที่เพิ่มใหม่ในหน้า Top-down ปรากฏในหน้านี้ และถ้าไม่มีข้อมูลปีก่อนใช้ Seasonality ของ Channel
5. แก้ % แล้วบาทเปลี่ยน แก้บาทแล้ว % เปลี่ยน กลับไปแก้เป้า Sub-channel ในหน้า Top-down แล้ว % รายเดือนคงเดิม
6. กำหนด Sales Person แล้วชื่อไปแสดงใต้ Account ในหน้า Top-down
7. อัปเดต `CLAUDE.md` (Sales Person master และ Key, โครงตาราง Phasing, Filter Channel)
